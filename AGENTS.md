# AGENTS.md

## Project

NovaSector is a downstream fork of `/tg/station`: BYOND Dream Maker (DM) backend plus React/TypeScript TGUI.

- `tgstation.dme` is generated. Never hand-edit its `BEGIN_`/`END_` include blocks; `code/genesis_call.dme` must remain the first include.
- Follow upstream tgstation direction and keep Nova changes modular so upstream merges remain tractable.

## Build and validation

Use the Juke build entrypoint; direct DreamMaker builds are unsupported.

```sh
tools/build/build.sh                  # DM + TGUI
tools/build/build.sh dm               # DM only
tools/build/build.sh tgui             # TGUI only
tools/build/build.sh tgui-test        # TGUI tests
tools/build/build.sh lint             # lint and type checks
tools/build/build.sh --ci lint tgui-test
```

On NixOS, enter `nix develop` first. TGUI uses Bun. Windows uses `BUILD.bat` or `bin/*.cmd`.

- DM unit tests live in `code/modules/unit_tests/` and are registered in `_unit_tests.dm`.
- `TEST_FOCUS(/datum/unit_test/...)` isolates a test locally; never commit a focus marker.
- CI also runs DreamChecker/OpenDream and repository-specific checks. SpacemanDMM forbids relative type/proc definitions and `:` type overrides.

## Modularization

Almost all Nova changes belong under `modular_nova/`; see `modular_nova/readme.md` and `modular_nova/mirroring_guide.md` for the full rules.

- New content: `modular_nova/modules/<module_id>/`; do not reproduce the core directory tree inside a module.
- Core overrides or added vars/procs: `modular_nova/master_files/`, mirroring the original `code/` path. Prefer `. = ..()` over copied upstream procs.
- Shared defines: `code/__DEFINES/~nova_defines/`. Single-file defines belong at the top and must be `#undef`'d at the bottom.
- Maps: never edit upstream `.dmm` files directly; use `modular_nova/modules/automapper`.
- Binary assets: never modify upstream binaries. Put Nova assets in the appropriate modular/master files location.
- Non-trivial modules need a `readme.md` based on `modular_nova/module_template.md`.

### Unavoidable core edits

Mark core changes precisely and document them in the owning module readme:

```dm
// NOVA EDIT ADDITION START - MODULE_ID
// NOVA EDIT ADDITION END
value = 2 // NOVA EDIT CHANGE - ORIGINAL: value = 1
```

Use REMOVAL/ADDITION blocks for multiline replacements. DM indentation is syntax: inside a proc, record a removed line with an indented single-line `NOVA EDIT REMOVAL` comment; never place a column-zero block-comment terminator inside a proc. Confirm core DM edits with a real DM compile.

### TGUI

TGUI remains under `tgui/packages/tgui/interfaces/`; there is no modular Nova TGUI tree.

- A new Nova UI file starts with `// THIS IS A NOVA SECTOR UI FILE`.
- Edits to upstream JSX/TSX use precise `NOVA EDIT` markers.

## Conventions

- `.dm`, `.json`, and `.md` use tabs; JS/TS and other files use spaces. Follow `.editorconfig` and `biome.json`.
- Player-facing changes add `html/changelogs/*.yml`; copy `example.yml` and use two-space YAML indentation.
- Treat player input as hostile. Revalidate context after prompts, parameterize SQL with `format_table_name()`, scope `locate(ref)` to an allowlist, and validate Topic href actions.
- New player-facing interfaces must use TGUI.
- Detailed style/security/map rules live in `.github/guides/`.

## Commits

- Keep subjects concise and describe the delivered change, not the conversation that requested it.
- Use a short body only when it adds essential rationale or compatibility notes.
- Never copy user prompts, chat transcripts, investigation diaries, model attribution, or routine validation output into commit messages.

## Layout

- `code/`: upstream/core DM.
- `modular_nova/`: Nova modules, overrides, and tools.
- `tgui/packages/`: frontend packages.
- `_maps/`: map definitions and configs.
- `config/`: server configuration.
- `tools/`: build, CI, map, asset, changelog, and i18n tooling.

## Internationalization

Full documentation: `modular_nova/modules/i18n/readme.md`; command reference: `tools/i18n/README.md`.

- `I18N_SERVER_LOCALE` selects the server locale; `en` is the no-op default.
- Runtime text uses `LANG("key", args)`. Catalog keys are content hashes: never edit them by hand. After changing player-visible English, run `nova-i18n extract`.
- `i18n.dm` must remain included early in `tgstation.dme`.
- Machine identifiers—IDs, action values, lookup keys, role names, icon states, refs—must stay canonical English. Send a separate localized display label and reject unknown submitted keys.
- `strings/i18n/policy.json` is the shared policy source for DM, TGUI, and Rust tooling. Sync TGUI after changing it.
- TGUI action values remain English. Localized choices use `{ value: englishId, displayText: localizedLabel }`; bare string options are intentionally not translated.
- Preserve `{0}` placeholders, HTML structure, and DM text macros in translations.

Primary commands:

```sh
nova-i18n extract
nova-i18n rewrite
nova-i18n labels
nova-i18n lint
nova-i18n pseudo
bash tools/i18n/pseudo-test.sh
bash tools/i18n/resync.sh       # after upstream merges; not CI
bun tools/i18n/mt/i18n-mt.ts
```

Debug i18n by category, not by patching isolated strings:

1. Run `nova-i18n lint` and the pseudo-locale checks first.
2. If translated UI text breaks an action, inspect identifier/display separation and `policy.json`.
3. If cataloged text stays English, identify whether the render path bypasses LANG/P1, extraction missed the source class, the translation is partial, or the build is stale.
4. Fix recurring misses in the extractor, rewrite rules, label sources, or policy rather than adding one-off entries.

Known trap classes:

- **Catalog entry exists but the UI still shows English** — compare the key byte-for-byte with what the runtime actually looks up. TGUI keys must be the *post-JSX-transform* string: `tgui-catalog.mjs` decodes HTML entities (`&apos;` `&nbsp;` `&ensp;` …) because `JsxText.text` keeps them raw while React does not. An entity in a catalog key is a dead key; an entity in a translation renders literally to the player. `tgui-catalog.mjs extract` warns on both.
- The TGUI English catalog is `new ∪ historical` and never prunes, so a bad-key class survives every re-extract until it is migrated explicitly.
- **按 proc 语义界定，不要按变量名穷举** — examine/`. +=` 类累加器原先靠一张手写变量名白名单，任何局部名（`how_cool_are_your_threads += "…"`）都会漏。`extract::ProcCtx` 改为按 proc 名判定 examine 家族；放宽准入时配一道整句闸门（`is_examine_sentence`），否则拼句碎片（`" and "`、`" (good)"`）会各自入目录，被 AC 层在半句处替换成语序错乱的中文。
- `. += span_notice("A") + "\n" + span_notice("B")` 这类**拼接链**，整条 `build_template` 会把兄弟片段变成 `{0}/{1}`，抽出改写侧永远跳过的废键（一行多个字面量无从定位）。按操作数拆开逐段抽。
- 英式/美式拼写会让 SINK_VARS 看着已覆盖实则全漏：`flavor_text` 在表里、`flavour_text`（幽灵角色入场文字）整类没抽到。
- 译文里的 DM 复数宏 `\s` 要**去掉**——中文名词后会渲染出多余的 "s"。目录既有约定即如此。
- **TGUI 混排 children 必须整条抽成模板** — `<Box>Reduced by {n}% when infected.</Box>` 的 children 是 `["Reduced by ", n, "% when infected."]`。逐段翻会按英文语序拼回去（「减少了 2 感染病毒时的%。」），中文语序不同 → 碎片翻译必错，比不翻更糟。`tgui-catalog.mjs childrenTemplate` 整条抽成 `Reduced by {0}% …`，`localize.ts localizeChildrenTemplate` 整条查表再回填占位符；占位符数量对不上就整条保持英文，绝不回退逐段翻。抽取期的空白处理要与 JSX transform 逐字节一致（`jsxTextValue`，Babel 同款规则）。
- `build_template` 对「去标签后不含字母」的表达式返回 `None`，这是一道**正经防线**（挡住 `VV_DROPDOWN_OPTION` 那种把 admin 操作标识符夹在 HTML 里的写法）。绕过它去拆内插时，必须限死「整条就是一个内插」的形状（lead 与各段尾巴皆空），否则会把 VV 面板的标识符抽进目录。
- 目录只合并、从不裁剪：跑坏一次抽取留下的脏键会一直留着（含新建的 `<ns>.json` 未跟踪文件）。改抽取规则时先 `git checkout strings/i18n/en` 再重抽，并用 `git status` 查未跟踪的新命名空间文件。
- **TGUI 下拉的英文是设计缺口、不是漏译** — P1（`lang_reverse_phrase_tgui`）对「本身就是 tgui 目录键」的负载值**故意保持英文**，把显示交给 TS 端 auto-localize，好让 `act()` 回传仍是英文标识符；单词串（`"Assistant"`）连多词门槛都过不了。但 `localizeOption` 又对**裸字符串选项**一律不翻（`m(o)=o`，翻了回传就变中文）。两侧各自正确、合起来就是「下拉永远英文」。修法在 `localize.ts localizeDropdownProps`：运行时把裸字符串升级成 `{value: 英文, displayText: 译文}`，并按 `selected` 补 `displayText`（Dropdown 收起时显示的是 value 不是 displayText）。识别靠 `type === Dropdown` 组件标识，**不能**按「有 options prop」猜——界面里自定义组件也叫 options（AdminFax/LogViewer 是 `string[]`）。
- **可翻 prop 名清单必须单一来源** — 抽取器与 `localize.ts` 各存一份时，新增 prop 只改一边 → 「目录有键界面不翻」或「界面翻了目录没键、MT 永远漏」。已收进 `strings/i18n/policy.json` 的 `translatable_props` / `option_text_props`。`Button.Confirm` 的 `confirmContent`（满屏「Confirm?」）就是这么漏了整整一类。
- **JSX 属性里的 `'A' + 'B'` 折行拼接**：JS 求值后是一整串，运行时按整串查表，逐个操作数抽出的半句是死键。`addDisplayExpr` 对 `+` 链做纯字面量折叠（`foldStringConcat`）；含表达式的拼接（`'Tank (' + moles + ')'`）形状不可复原，**整条不抽**——同混排 children 的道理。
- **`capitalize()` 显示层把整类译文打掉** — DM 惯例「小写存、显示时 `capitalize()`」（手术名、伤口/器官/试剂名、`"[capitalize(x.name)]"` 拼句），而目录键保留源码原样的小写 → 精确反查与 AC 字典双双 miss。`lang_build_reverse` 为**多词**小写键登记首字母大写变体；单词键**不登记**（`move`/`clear`/`ready` 是标识符形态，会把 `switch("Clear")` 拖进反查面——与 P1 多词门槛、AC 多词过滤同一条安全线）。单词类仍需落地点收口。改了这条变体规则要同步 `lint.rs` 的碰撞集合，否则门禁看不见新暴露面。
- **DM 单词显示名到不了前端** — P1 对「本身就是 tgui 目录键」的负载值故意保持英文（留给 TS 只翻显示、`act()` 回传仍是英文），而单词串连多词门槛都过不了。于是 `mutation.Name` 这种「前端既显示又当标识符用」的值，译文明明躺在 `datum.json` 里却永远显英文。修法不是改 P1，是把该类型的 name 经 `labels.rs TYPE_VAR_RULES` 桥进**前端目录**（按类型路径，覆盖全部子类型、上游移动文件不失效）。判据：前端是否拿它做比较/取 ref（`m.Name === name`、`Name !== 'Monkified'`）——是则必须走这条桥，绝不能让 P1 改数据。
- **AC 自动机一律要 LeftmostLongest，默认 Standard 取最短匹配** — 这条踩过两次，第二次代价大得多。字面 AC（fallback.dm）早就换了；**模板逆匹配引擎的锚自动机漏了**，还留着一句「重叠锚被遮蔽只是少收一个候选、不影响正确性」的注释——不成立，少收的正是对的那个：`" begins to make an incision in "` 把 `" begins to make an incision in the organs within "` 整个遮住，更短的通用锚还能把两条一起遮住，于是唯一匹配得上的模板根本不进候选，整句原样留英文。所有插值句（手术每一步的可见消息、examine 拼句…）都吃这一刀。`i18n_real_catalog` 守这条。
- **tgui.json 也在全局反查表里** — 「值兼标识符的显示词就抽进前端目录、让 TS 只翻显示」这条路对**多词**成立，对**单词**不成立：`build_i18n_cache` 扫 locale 目录下**全部** .json，tgui.json 也在内，所以往里塞 `blue`/`purple`/`gold` 等于毒化整个 DM 侧的 `lang_reverse_text`（P1 有 `i18n_tgui_strings` 守卫，`lang_reverse_text` 没有）。线缆颜色这么塞过一次，被 `i18n_real_catalog` 的「**不应**进反查表」断言当场抓住。单词类显示词要么走域内表（`lang_scoped_table`），要么在 ui_data 里另发一个显示字段（线缆最终用后者：`shownColor` 继续当 CSS 颜色名与 act 值，另加 `shownColorLabel` 供前端作 label）。
- **悬空 LANG key 比不翻译严重得多** — 抽取与改写是两条独立通道，任何「让 extract 跳过、rewrite 不跳过」的规则都会产出 `LANG("obj.b045da9c")` 而目录里没这条；`lang_resolve` 兜底**返回 key 本身**，玩家看到的就是这串乱码。一次实测全仓三万余处调用里有 76 个（耳机频率表、无人机分发器、血虫技能、雇佣合同…）。`nova-i18n lint` 现在把它当**错误**扫。要回填原文：`git log -S<key> -- <file>` 找引入 commit，从 diff 的 `-` 行取字面量，用 `nova-i18n key <ns> <tpl>` 的 hash **校验**再写回（76 条里 72 条能这么自动恢复，剩下的是多行续行串，手拼后同样按 hash 验）。注意占位符按**出现次数**编号：同一个 `[employee_name]` 出现两次就是 `{0}` 和 `{1}`。
- **假数据测不出真目录的坑** — `i18n_template_match` 注合成模板验证引擎逻辑、`i18n_unreverse` 验证反查往返，两者全绿，而真目录里那条就是翻不出来（锚遮蔽只在成千上万条真锚互为前缀时才发生）。要有一个**拿真目录、按真实渲染形态**跑落地层的测试（`i18n_real_catalog`），并且**分层断言**（目录 → 引擎就绪 → 锚命中 → 裸句 → 带 span 整条），否则只知道红了不知道断在哪一层。
- **改写把字面量抬成 LANG 实参后，没有任何抽取路径认得它们** — `"The [x ? "bolt" : "screw"] is …"` 改写成 `LANG(key, list(x ? "bolt" : "screw", …))` 之后，那两个字面量既不是 sink 实参也不是累加器右值。模板译了、`lang_localize_arg` 拿实参去查却查不到 → 整句里嵌着英文（全仓 800+ 调用点）。抽取时走 LANG 实参子树，但**必须绕开下标键**（不下探 `Follow::Index`，否则 `ded["name"]` 被译、取值 miss）和**嵌套 LANG 的 key**（按 `<ns>.<hash>` 形态挡）。同样**只收多词**：单 token 实参里 act/topic/wire 键、黑板键、全大写常量浓度极高，放开一次就是 12 条高置信碰撞。
- **运行期 `X.desc += 后缀` 会连基础句一起打掉** — 拼接后整串不是目录键，精确反查整条 miss（高优先级赏金三条整段英文即此，基础句本来早就译好）。两侧都要补：抽取侧认 `X.desc/description +=`（原有累加器规则只看裸标识符），运行侧把后缀登记进 `i18n_appended_suffixes`、由 `lang_reverse_suffixed` 拆开分别反查，并让 `lang_reverse_phrase_tgui` 兜住它。接缝空白是暗礁：有的后缀源码自带前导空格，有的靠 DM 续行（`"</br>\` + 换行 + 制表符），抽取器与 BYOND 未必逐字节一致 → 测试要**照抄源码的续行写法**构造被测串（`i18n_suffixed`），别手写等价物。
- **`lang_localize_arg` 的 capitalize 兜底会撞上同形异义词** — `smell`（名词，污染物 descriptor，`#define` 不在目录）→ 兜底 capitalize 成 `Smell` → 命中动词条目「闻」→「烟细微的闻让你的鼻子发痒」。同形异义的显示词要进 `_state_words.json`（查表第一步，先于兜底）钉死词性。
- **MT 会吃掉纯 ASCII 译文里的字符** — `H.A.R.S.` → `H..R.S.`、血型 `A+` → `+`。查法：扫「译文不含 CJK、比原文短、且是原文的字符子集」，全仓一遍只有个位数，其中冠词/复数类是有意的，缩写/型号类是 bug。
- **共享常量表住在 .ts 里，`walk()` 只扫 .tsx/.jsx** — `constants.ts` 的 `GASES` 经 `getGasLabel(gas_id)` 渲染进一整排大气界面，界面文件里没有任何字面量 → 整类漏抽。按「文件+表名+字段」定点登记（`CONSTANT_LABEL_TABLES`），不整体放开 .ts（backend/logging 里的 name/label 多是标识符）；`id`/`path` 是回传标识符，永不入表。
- **上游把逻辑搬进新组件文件 = 整类落地点静默回退英文** — 上游把板条箱隐私锁重构成 `/datum/component/locked_to_account` 后，消息经**项目自定义 proc**（`deny(source, user, msg)` → 内部 `to_chat(span_warning(msg))`）下发。extract 认得这些字面量（照常进 `datum.json` 并被翻译），rewrite 却不认 `deny` 这个非注册 sink → 源码留裸英文、目录里躺着永远查不到的译文。**「目录里有、且已翻译」不等于「玩家看得到」**：同文件里 `balloon_alert` 那行照常被改写，对比之下更隐蔽。上游同步后的排查手段是拿「不再被引用的旧 key」反查其英文原文在源码里是否又以裸字面量出现（仅凭 `nova-i18n lint` 查不出——它只查悬空 key，查不出「该 LANG 而没 LANG」）。
- **同一次 rewrite 可以既漏抽又生成悬空 key** — 上游把 cyborg examine 的盖板句拆成 `var/cover_message = "…" ` + 两段 `+=` 后：基础句因是**局部变量赋值**（非 `. +=` 累加器）整条没抽 → 裸英文；而 `+=` 的 href 段被 rewrite 改成了 `LANG("mob.4b3e8678")`，extract 却因含 `<a href=…>` 跳过 → **悬空 key，玩家直接看到 `mob.4b3e8678` 这串乱码**。两个方向的缺口出现在**相邻三行**里。`nova-i18n lint` 只抓得住后者，前者要靠回归门禁的裸英文反查。恢复悬空 key 的原文去 `git show upstream/master:<file>` 取（这类 key 是本次 rewrite 新生的，`git log -S` 查不到引入 commit）。
- **前端把 payload 的 `name` 拼进 act 动作串 = P1 一译按钮就哑火** — `payload_skip_keys` 只保护「值本身就是回传标识符」的键（`buttons`/`items`/`id`/`ref`…），`name` 不在其中且**本该不在**（绝大多数场合 name 就是纯显示）。危险形状在 TS 侧：``gear_action: `toggle_reagent_${reagent.name}` ``——P1 把 `name` 译成中文后回传 `toggle_reagent_生理盐水-葡萄糖溶液`，DM 侧 `action == ("toggle_reagent_" + known_reagents[i].name)` 拿英文比，永远不等 → 按钮点了没反应、无任何报错。**单词名（Epinephrine/Multiver）因多词门槛不被译而照常工作**，于是表现为「只有多词化合物坏」，极易被当成个别条目漏译。修法不是把 `name` 加进 skip keys（会让整界面回退英文），而是**另发一个 `id` 字段**（`id` 已在 skip keys 内，天然免疫）供拼动作串，`name` 继续只做显示。全仓扫法：``grep -rn '`[a-z_]*\${[a-zA-Z_.]*\.name}`' tgui/packages/tgui/interfaces/``（2026-08-13 只有机甲注射器枪/机甲睡眠舱三处）。
- **React 不渲染的 children 占了模板占位符 = 整类「目录有译文、界面永远英文」** — `<div>{text}{cond && <Divider/>}</div>` 在 cond 为假时 children 是 `[原文, false]`。`localizeChildrenTemplate` 旧实现按「非字符串 = 一个占位符」建模板，于是查的是 `…station.{0}`，目录里当然没有 → 未命中后又撞上「混排就整条保持英文」的保守分支，把它焊死。React 对 `null/undefined/boolean` 什么都不渲染，这类空位必须在建模板**之前**剔除。反派介绍 tooltip 整页英文即此（译文一直躺在 `tgui.json` 里）。判据：目录里有键有译文、但界面是英文，且该处 JSX 有 `{cond && …}` 兄弟节点。
- **「混排就整条保持英文」把「运行期整条文案 + 装饰性兄弟节点」也一起焊死** — 上一条剔掉了 `false` 空位，但只救了**末段**；真正的漏洞在保守分支本身。`<Box>{icon}{tab.name}</Box>`（配装页分类页签）、`<div>{desc}<Divider/></div>`（反派介绍非末段）、`<>{name}<span>{n} slots available</span></>`（中途加入菜单部门标题）这三种形状里，字符串是**运行期数据**、整条模板永远不可能在目录里，于是全部回退英文——而它们各自的英文原文本来就是独立目录键、译文一直躺着。判据是「同一条 tooltip 第一段英文、第二段中文」这类**同形状不同待遇**的反差（末段 `false` 被剔除后走了纯文本路径）。修在 `localize.ts localizeChildrenSegments`：模板未命中后允许逐段翻，但闸门要双重——每个非空白字符串 child 都必须**整条精确命中目录**（拼句碎片按定义不是独立键，抽取器只存整条模板），且**首字符不是小写字母/标点**（目录里确实躺着 `and give it a`、`a mindshield.` 这类碰巧能命中的续接碎片）。`localize.test.ts` 正反三条守这条。
- **拼句碎片进字面 AC 子串字典 = 从单词内部开火** — 反查表同时喂两条路：`lang_reverse_text` 的**整串精确**反查（碎片在那里无害）和字面 AC 的**子串替换**（碎片会在任意句子中间开火）。rustg 的 AC 没有词边界概念，LeftmostLongest 只管「同起点取最长」。旧闸门只要求「pattern 含空格」，于是 `"one of"→"其中一只"`（一条**没有调用点的悬空目录项**）把 `But n|one of| its eggs hatched!` 咬成「But n其中一只 its eggs hatched!」；`" and "→" 和 "`（靠首尾空格才含空格的单词碎片）污染整段 NPC 检查文本。闸门只能设在 `lang_fallback_setup` 建字典这一步（`lang_fallback_pattern_safe`：trim 后须多词；无句末标点的 ≤3 词短语若首/尾是虚词则拒收），碎片仍留在目录里供各自调用点精确查表。`i18n_ac_fragment` 守这条。
- **目录键里嵌着 HTML 标签 → 聊天落地层永远查不到** — 抽取器照抄源码字面量，标签就留在键里；而 `lang_fallback_apply_html` **按标签切块**、只把标签之间的纯文本送去查表。两种形态各需一处修：
  · **边缘标签**（整句被包住，`"<b>But none of its eggs hatched!</b>"`、`"<span class='notice ml-1'>Subject contains no neuroware…</span>"`）→ `lang_build_reverse` 登记**剥标签变体键**（值同样剥标签，外层标签由切块器自己保留）。与既有的剥宏/去转义/首字母大写变体同一条流水线、同一条「只做多词」安全线。
  · **句中内联标签**（`examine_text = "There is a sticker displaying the <b>Chief Engineer's SEAL OF APPROVAL.</b>"`）→ 切成两个半句，谁都不是键 → `lang_fallback_apply_html` 前置一遍 `lang_localize_inline_runs`：跨内联标签把整段文本连起来整段精确查表，命中才替换、未命中原样交还切块器（不新增误翻面）。含 `<script>/<style>/<textarea>` 的文档整个跳过这条前置 pass。
    **run 必须是元素的「内容」，不能跨过该元素自己的边界标签**：只靠 depth 计数不够——run 从外层 `<span class='notice'>` **之前**就开始的话，该 span 会被吸收进 run、它的闭合又把 depth 抵平，于是整条替换把 span 一起吃掉、聊天配色全丢。判据要加一条：run 至今没有任何非空白文本时遇到的**开标签**属于外壳，让它当边界、run 从它之后重新开始。这条是 `i18n_html_tag_keys` 实测抓出来的（译文正确但 span 没了），静态看代码看不出来。
  症状特征：整句英文，且句中某个专有名词被单独译成中文（AC 只咬中了那一个词组）。`i18n_html_tag_keys` 守这两条。
- **`examine_tags` 是全仓唯一「assoc 键即文案」的合法形状** — 该 proc 返回的 list，**键**是检查面板上那颗标签的文字、**值**是它的悬停 tooltip。抽取器原本只抽值，于是直接写字面量当键的写法整类漏掉（`examine_list["partially EMP blocking"] = …`）；用 `EXAMINE_TAG_*` 宏的那批因为宏本身是标签文字而早就在目录里，对比之下更隐蔽。这条只能开在本 proc 语境内——别处的下标键一律是程序查表用的键名，`visit_expr` 对 `Follow::Index` 的整支跳过必须保留。
- **proc 形参默认值是 SINK_VARS 够不着的一类**（已修） — `Initialize(revive_title = "a recovered crewmember", spawn_text = "Recovered Crew", …)` 这种把玩家可见文案写在**形参默认值**里的组件（ghostrole_on_revive 等），SINK_VARS 走的是类型变量声明，一条都抽不到 → 玩家看到「你想扮演a recovered crewmember吗？」。放开时的误伤担忧是真的（`name`/`message` 作形参名时标识符浓度远高于作类型变量时，`proc/f(message = "some_key")`），解法是**在 SINK_VARS 之上再加一道多词闸门**（复用 LANG 实参的 `is_lang_arg_text`），单 token 默认值一律不收。实测全仓只新增 14 条、逐条人工过目零标识符混入——**先量化再决定**，这个量级本身也证明闸门没开太大。
- **`{' '}` 被当成占位符 = 一次毒掉 90 条模板 key** — prettier 换行时到处插 `{' '}`（`The <b>Linguist</b>{' '}` + 换行 + `neutral quirk …`）。React 把它渲染成一个**字符串** child，运行时 `localizeChildrenTemplate` 会把它并进模板文本；而抽取器 `templateChildren` 原本对 `ts.isJsxExpression` 一律记 `{slot:true}` → 算出的 key 比运行时多一个 `{N}`（`The {0}{1}neutral quirk` vs 运行时的 `The {0} neutral quirk`）→ 整条模板永远查不到 → **整段回退英文**。判据/症状：整段英文，但段落里的 `<b>`/`<span>` 词是中文（它们是独立 jsx 节点、各自 auto-localize 命中）——与「AC 只咬中一个词组」的 HTML 标签类症状很像，区别在这里被译的是**元素子节点**而非任意词组。修在抽取侧：JSX 表达式里是字符串字面量（`StringLiteral`/`NoSubstitutionTemplateLiteral`）时按**文本**处理。旧的错误 key 会永远留在只增不减的目录里，新 key 需重新翻译。`localize.test.ts` 用「按源码 JSX 形状独立构造 children、断言命中真目录条目」的方式守这条（不从 key 反推，否则是循环论证）。
- **落地层的层序：整串精确反查必须排在模板引擎之前** — `lang_fallback_apply` 原本是「模板逆匹配 → 字面 AC」，没有独立的整串精确反查那一步（整句只能靠 AC 顺带命中）。于是目录里那些「三两个词 + 占位符」的**泛化骨架模板**会抢在前面把整句劫持，把捕获到的英文原样塞回中文脚手架：`It appears to {0}`→`它看起来像{0}` 把「It appears to be completely inactive. The reset light is blinking.」吃成「它看起来像be completely inactive.」；`{0} produces a {1}.`→`{0}产出{1}。` 把「Fully heals the target and produces a random coin.」吃成「Fully heals the target and产出random coin。」。**两句的整句译文一直都在目录里**，只是永远轮不到。这比不翻更难看（中文脚手架裹着英文、语序还错），而且**不能靠收紧模板锚解决**：真正该留的手术类锚（`" begins to make an incision in "`）同样以介词结尾、词数门槛也会误杀 `{0} succeeds!` 与 `Prevent {0} from escaping alive.`。正解是让最具体的证据优先——整串命中就直接返回，模板与 AC 都不再跑。症状特征：中文句式里裹着成段英文（区别于 AC 碎片类的「英文句里嵌一个中文词」）。`i18n_real_catalog` ①c 守这条。
- **模板的字面段里嵌着 HTML 标签 = 整条在聊天路径上永远验证不过（全仓 859 条）** — `lang_fallback_apply_html` **先按标签切块**，送进模板引擎的是**纯文本块**；而 `lang_tpl_match` 要求逐段 `findtext` 命中**带标签的**字面段，于是这类模板一条都匹配不上。译文早就在目录里，只是这条通道走不通。实测：15906 条已译插值模板里 859 条（5.4%）是这形状——高级健康扫描仪整页（`<span class='info ml-1'>Genetic Stability: {0}%.</span><br>`）、回合总结经济行（`There were {0} {1} collected by crew this shift.<br>`）皆在其中。解法是在 `lang_tpl_setup` 里给这类模板**额外登记一条剥标签变体记录**去匹配切块后的纯文本，外层标签由切块器自己保留、排版不丢。两条硬约束：
  · **含 `<a>` 的一律不登记剥标签变体**（71 条）：剥掉链接会把功能弄没（`<a href='byond://…'>here</a>` 是投票入口）。这批改走**整行作用域**——`lang_fallback_apply_html` 在**切块之前**先对整行跑一遍 `lang_template_apply`，那时字面段里的标签与原文逐字节对得上，zh 模板连同自己的 `<a href>` 一起填回去，链接与排版都保住。**作用域选错才是原来的死结**，不是「剥不剥标签」的取舍。整行 pass 同样让 788 条纯排版模板优先在完整形态下命中，剥标签变体退化为切块后的兜底。
  · 剥标签时**不能 trim/折叠空白**（要另写 raw 版，别复用给反查变体用的那个）：字面段靠精确 `findtext` 定位，段首那个分隔占位符与词的空格一旦被 trim，整条模板反而再也匹配不上。
  `i18n_html_tag_keys` ③④ 正反两面都守（③ 断言剥标签变体命中，④ 反向断言投票行**必须**保持英文）。
- **LANG 实参里「本身就是插值句」的那类，抽取器整段丢弃** — `collect_lang_arg_literals` 对 `Term::InterpString` **只下探内插表达式、把字面文本全丢掉**，于是 `ask_role ? "Personality requested: \[[ask_role]\]" : ""` 这种被改写抬成 LANG 实参的插值句，一个字都进不了目录：外层模板译好了、句子中间嵌着一截英文。修法是加一条平行的**模板**收集器，按 `Personality requested: {0}` 的形态入目录，运行期由整行模板引擎命中并递归本地化捕获值。安全线：**只收带占位符的模板**（永不进反查表，且要求全部字面段按序命中，比裸串反查安全得多）+「去占位符后须多词」挡掉 act/黑板键。全仓 87 条。
  连带坑：抽出的是 `Personality requested: \[{0}\]` —— DM 源码里 `\[` `\]` 是「字面方括号」的转义（否则被当成内插），而运行时是裸括号。`lang_tpl_normalize` 原本只归一 `\"` `\n` `\t` 与文法宏，**漏了这两个**，不补则字面段照样对不上、等于白抽。
- **地图里定义的 `desc` 覆盖，整类不在目录** — `.dmm` 里的 `desc = "…"` 是**实例变量覆盖**，不在任何 `.dm` 源码里，dreammaker 解析器（`nova-i18n extract`）根本看不到 → 整类漏抽。**迷惑性极强**：物体的 **name 是中文**（地图名早就手工收进 `_map_names.json`）、**desc 却是英文**，看着像「这一条漏译」，实际是「这一整类从没进过目录」；而且在 `.dm` 里怎么 grep 都找不到那句英文（线上实例：heretic.dmm 的「充满恐惧的人」）。判据：`grep -rl "<那句英文>" _maps/`。产物由 `node tools/i18n/map-descs.mjs` 生成 `strings/i18n/en/_map_descs.json`（identity 表，与 `_map_names.json` 同形态，只合并不裁剪），resync 之后跑一次。
- **P1（TGUI 负载）缺字面 AC 兜底 → 「基础句 + 运行期后缀」整段英文** — `lang_reverse_phrase_tgui` 的链是「精确反查（含 `lang_reverse_suffixed`）→ 模板引擎」，**没有 AC**。而 TGUI 负载里的追加后缀不止可枚举那种：幽灵生成器的 `flavour_text` 是`基础句` + `switch(rand(1,4))` 四选一的身世段（其中一段还带 `pick(...)` 内插），后缀根本没法手工登记进 `i18n_appended_suffixes`。**两半各自都是目录键、也都译好了**，字面 AC 的子串替换正好能分别换掉、接缝原样保留——聊天路径一直这么做，P1 少了这一步。症状：生成器菜单里「来源」是中文、「指令」整段英文（同一条负载两个字段待遇不同，这个反差就是判据）。闸门必须严：AC 是子串替换，**长度 ≥ 80 且含句末标点**（散文形态，不可能是 act 回传标识符）才放行，短值一律不走——那是标识符浓度最高的区间。
- **`span_*()` 包裹的 LANG 实参，`lang_localize_arg` 每一步都 miss** — 改写后的调用形如 `LANG(key, list(span_bold("[read_only ? "protected" : "unprotected"]")))`，运行期传进来的是 `<b>unprotected</b>`：整串既不是状态词、也不是目录键，于是中文句子里嵌着一个英文状态词（软盘的「写保护标签设置为unprotected。」）。`lang_localize_arg` 现在会剥掉**首尾包裹标签**、对内层递归本地化，命中后把标签原样套回去（加粗等排版不丢；内层已无标签，递归必然终止）。
  另一半是数据：`protected`/`unprotected` 是**单词**，被 LANG 实参的多词闸门挡掉——那条闸门不能放开（单 token 实参里 act/topic/黑板键浓度极高）。这类「同形异义状态词」的正确去处是 `_state_words.json`，它只在 LANG 实参/模板捕获这个受限范围内生效，不会污染全局反查表。
- **整句在 TypeScript 里拼 = DM 侧模板再全也没用** — 偏好菜单的个性总结 `You are ${finalString}.` 完全由 TS 拼出（`PersonalityPage.tsx`）：整串是运行期产物、永远不是目录键，而个性名是**单词**（Compassionate/Diligent…）也进不了字面 AC → 两条落地路径都够不着。顺带一提 DM 侧那条 `You are {0}.` 的锚只有 8 字符、低于 `I18N_TPL_MIN_ANCHOR`，本来也进不了模板引擎。
  修法是让它走**既有机制**：名字逐个 `translateCurrent` 查表（`/datum/personality name` 早由 labels.rs 桥进前端目录），外框写成 **children 模板** `You are <span>{finalString}</span>.` —— 抽取器收成 `You are {0}.`、运行时 `localizeChildrenTemplate` 整条查表后按中文语序回填。
  **绝不能写成 `You are {finalString}.`**：那样三个 children 全是字符串、会被并成一整条查不到的文本（与 `{' '}` 那条同一个坑）——必须把它包进一个元素才算一个占位符。连接符也要按 locale 走（中文顿号、无 "and"），否则是「甲, 乙, and 丙」。
  另一种「整列英文」的成因完全不同：**该类型的 name 从没进过任何目录**。强化+ 页的体表标记名（`/datum/body_marking`）不是 SINK_VARS 覆盖的形状、也不在 `labels.rs TYPE_VAR_RULES` 里 → `en/datum.json` 与 `en/tgui.json` 都查不到。判据一步到位：拿一个界面上的英文名去 `grep strings/i18n/en/`，**一条都没有**就是整类漏抽（对比「个别条目漏译」是目录里有键、值等于原文）。补一条 TYPE_VAR_RULES 即可，前端已是对象选项时 value 保英文、只有 displayText 被翻。
  同类：船员名单的 `title={dept + \` (${open} positions open)\`}`（`CrewManifest.jsx`）。改法一样——部门名包进 `<span>` 当占位符（自身是独立目录键、auto-localize），外框整条抽成 `{0} ({1} positions open)`。**prop 值里的 JS 拼接一律是这个坑**，`title`/`content` 这些可翻 prop 只做整串精确查表，拼出来的串永远查不到。
  prop 拼接全仓 151 处，逐处改 children 模板并不通用（`placeholder`、`Window title` 只吃字符串），所以走**抽取模板 + 运行期逆匹配**：`tgui-catalog.mjs propTemplate` 把 `` `Reading: ${x}` `` 收成 `Reading: {0}`，`localize.ts matchPropTemplate` 在精确查表 miss 之后按字面段整串逆匹配、回填捕获值。三道闸门缺一不可，且都是实测逼出来的：
  · **准入面用 sidecar 隔离**（`strings/i18n/tgui-prop-templates.json`，每次抽取全量重写、**不合并**——它是误翻面不是译文）。目录里另有 590+ 条 children 模板，它们本就走整条精确查表；把 `- {0}, the {1}`、`{0} of 12 total` 这种泛化骨架放进逆匹配面就是 DM 侧「中文脚手架裹英文」那一跤。
  · **抽取期要求锚里有实词**：字面段全是虚词/标点的骨架（`{0} from {1}, {2}`，锚只有 `" from "` 与 `", "`）实测能吃掉目录里 27 条正常整句。
  · **运行期要求捕获值像「值」不像散文**（≤60 字符、不跨句、多词时不以小写开头、末尾不吞句号）。这条只能设在运行期：抽取期看不出 `Select {0}` 的 `{0}` 将来会被喂进什么，而 `Select ` 是正经实词却又是极常见的句子开头——目录里 17 条 `Select a policy to view. These policies are…` 全靠这条挡住。
  两道闸门加完，能被劫持的目录整句从 44 条降到 0（剩余匹配全是「本身就是目录键、精确查表先命中」的良性形状）。审计手法：拿目录里所有非模板 key 逐条过一遍逆匹配引擎，看有多少被吃掉。
- **`initial(x.name)` 拼出来的列表 = 只有多词项被译的「半译列表」** — 储物袋检查的「可以容纳:」每项是 `"\a [initial(valid_item.name)]"`：`initial()` 取**英文原名**（改成显示边界翻译之后，运行期 `name` 同样是英文），整条描述又是运行期拼的、不是目录键 → 落地只剩字面 AC，而 AC 按安全线**只收多词** → 多词名（circuit board→电路板）命中、单词名（limb/beaker/bottle/assembly）整类漏掉。玩家看到的「a limb / a 电路板 / a beaker」混排就是这条界线的直接投影，很容易被当成个别条目漏译。修法：在 `set_holdable()` 用 `. = ..()` 之后**重建**该描述，逐项走精确反查（单词也能命中、且不存在 AC 的词内开火风险），命中项**丢掉 `\a` 冠词**（中文无冠词，留着就是「a 电路板」）。同理适用于任何 `initial(name)` 拼列表的地方。
- **`%VAR` 宏占位公告：模板译了、值没译** — `announcement_system.dm` 的公告模板用 `%PERSON`/`%RANK` 而非 `{0}`，整条模板早有 Nova 反查编辑，但 `%VAR` 的**值**由调用方传入英文 → 「Feng Xin Zi 已注册为 Detective」。职位名多为单词（AC 够不着）、公告整串又是运行期拼的（不是目录键），两条路径都不行，只能在替换处逐个值过 `lang_localize_arg`。**必须在 proc 内部改**：`. = ..()` 拿到的是替换完的串，无法再区分哪段是值。公告是纯显示、无 `act()` 回传，值被译不破坏任何比较。
- **按序 `replacetext` 填占位符 = 实参自吞** — `lang_interpolate` 原本按序 `replacetext("{0}")`、`replacetext("{1}")`…，于是**上一轮写进串里的实参内容会被下一轮当成模板再扫一遍**：只要某个实参的值里恰好含 `{1}`（纸张文本、玩家自定义命名、任何玩家可控串都做得到），它就会被后一个实参顶掉。已改为单趟扫描（实参写进输出后不再参与匹配），顺带省掉「模板里没有该占位符时仍白跑一遍 `lang_localize_arg` + 全串 replacetext」的开销 —— LANG 是全仓三万余处调用的热点。`i18n_interpolate` 守这条。**测试实参必须用生造词**（Zxqv 系）：locale≠en 时 `lang_interpolate` 会对文本实参跑 `lang_localize_arg`，真实英文词（`stick`/`tail`）在真目录或伪 locale 下会被译掉，断言就从「测填充逻辑」变成「测目录内容」。
- **逐字节 `copytext(s, i, i+1)` 扫描在落地层是真开销** — `lang_fallback_apply_html` 是**每条 to_chat、每个浏览器页面**的必经路径，而它的内循环 `lang_html_tag_end`（找标签结束的 `>`，且要跳过引号里的 `>`）原本按字节推进，每字节分配一个新字符串；记录台/健康扫描那种几十 KB、上千标签的页面光这里就是几十万次分配，且**跑两遍**（内联 run 前置 pass + 切块器各一次）。DM 516 的原生 `spantext`/`nonspantext`（「从起点开始有多少个连续字符属/不属于某字符集」）与 `findtext` 直接顶掉这类循环，逐字节行为等价。同类还有 `lang_html_tag_parts`／`lang_html_raw_text_tag_name` 的标签名扫描、`lang_localize_inline_runs` 里「run 至今有没有非空白文本」的 `length(trim(copytext(...)))`（两次分配 → 一次 `spantext`）。注意 DM 字符串里**没有 `\r` 转义**（写了直接编译报错 undefined text macro）。另：`trim()` 上游已经委托给原生 `trimtext()`，不必再手工替换。
- **`name`/`desc` 是 `appearance` 字段，别在 `Initialize` 里原地反查** — 旧做法在 `/atom/Initialize` 与（不调父级的）`/turf/Initialize` 里把英文名/描述整串反查成译文，覆盖面最大，但代价是地图加载期对约 128 万实例各做两次外观变更（churn；appearance 内化+引用计数，同型实例仍共享一份，所以**不是**「每实例一份外观」——写文档时别把这点夸大成结论）。已改成实例保留 canonical English、只在显示边界翻：`/atom/get_examine_name`、`/atom/examine` 的 desc、`/atom/MouseEntered` 的 hover screentip，统一走 `lang_localize_name_for_display`。三条连带规律：
  · **分清「这次的回退」与「历来如此」**：`/datum` 的 name（材料/试剂/设计/配方/货运包）从不走 atom 的 Initialize 钩子，那些界面的单词名是既有状态；这次真正影响的只有「obj/turf 的 name 直接进 TGUI 负载」这一小类，而高流量那几处早有定点本地化。已补的两处宽覆盖边界：`tgui_input_list`（选项文本反查 + `items_map` 用显示串作键 + `default` 换显示形态，往返由 `i18n_display_boundary` 守）与径向菜单切片 `name`（标识符走 `E.choice`）。
  · **代价只在「名字不作 LANG 实参」的路径**：聊天不受影响——`[src]` 早被 rewrite 抬成 LANG 实参（`list(src` 一种形状全仓 3000+ 处），`lang_localize_arg` 是逐实参精确反查、**没有多词门槛**，单词名照样命中。真缺口是 TGUI 负载单词名、径向菜单 tooltip、状态栏、`tgui_input_list` 选项：那里只剩字面 AC 与 P1，两条都卡多词（`lang_reverse_phrase_tgui` 见无空格值直接返回；`lang_fallback_pattern_safe` 要求 trim 后多词）。量级：名字形已译条目里单词占约 10%（2122/21174），多词名仍被覆盖。补法是按类型桥进前端目录（`labels.rs TYPE_VAR_RULES`）或域内表 / ui_data 另发显示字段，**不是**把原地反查加回来。审计时别只看「界面英文」就判缺口，先确认该处名字是不是 LANG 实参。
  · **mob 的判据只用 `initial(name)`**：`name` 偏离即身份名（角色名/宠物挂牌/赛博编号/ERT 头衔）一律不翻，等于 `initial(name)` 才是类型标签。别另设「是否被改名」标志位——它只覆盖走 `fully_replace_character_name` 的那条路径，还得跟父级的 early-return 保持同步（父级 `oldname == newname` 时会 return FALSE，标志位却已经清了），比这条判据弱。
  · **`lang_reverse_text(initial(name))` 这类补偿代码分两种，别一刀切**（全仓 44 处，撤了 7 处、留了 37 处）：注释理由都是同一句「`initial(name)` 会覆盖掉 Initialize 反查好的中文名」，钩子删掉后该理由全部作废，但**去留看实例名有没有英文语义**。撤：名字参与英文解析/比较的（火警器与防火门的 `"[区域名] [类型名] [id_tag]"` 按英文解析区域前缀、异种的防重名重置）——留着就是 memory 里「机器丢区域前缀」那一类。留：**上游本来就在运行期写 `name`** 的（MMI/posibrain/礼物/尸袋/项圈挂牌/蜂/无人机…），那里不存在「多一次外观写入」的代价，而拼句碎片（`"casing"`、`" This one is spent."`）没有别的落地手段，撤掉纯亏。撤 mob 侧的之前先确认边界认得 `set_name()` 形态（`"类型名 (编号)"` 翻前缀留后缀），否则例检当场退回英文。
  · **注入 locale 的单测必须在 `Destroy()` 里恢复全局**：`TEST_ASSERT` 失败即 `return`，恢复写在 `Run()` 末尾就会把合成 locale 留在 `GLOB` 里，之后每个 i18n 测试连带染红。`i18n_display_boundary` 守这条边界（正反两向：静态类型名要翻、身份名与 `TRAIT_WAS_RENAMED` 不许翻）。
- **「name 兼作 act 标识符」的单词显示名：走前端目录桥，且只收单词** — 这类 name（`/datum/vote`、`/datum/ai_module`、`/datum/chemical_reaction`、`/datum/design`、`/datum/material`、`/datum/reagent`）在 TGUI 里既显示又当回传键/客户端比较键，DM 端不能改数据，只能进前端目录让 TS 只翻显示（`labels.rs SINGLE_WORD_TYPE_VAR_RULES`）。**按词数分叉是硬约束**：多词 name 本来就被 P1 在负载里翻好了，塞进前端目录反而让 P1 按「本身是 tgui 目录键」跳过、改由 TS 翻，一旦该界面把它渲染在非可翻位置（模板串、非 translatable prop）就从中文退化成英文；单词 name 连 P1 的多词门槛都过不了、本来恒为英文，进目录是纯增益。配套安全线在 `tgui-catalog.mjs extract`：单词键的译文**只许沿用其它命名空间的既有词对**（`reverseZh`），`phraseTranslation` 现编的值一律不收——`tgui.json` 会被 `build_i18n_cache` 扫进 DM 侧**全局反查表**，凭空多出的单词词对就是扩大全局误翻面。审计手法：抽取后逐条比对「新键的 en→zh 是否已存在于其它命名空间」，实测 506 条新键里 462 条沿用既有、0 条新造。
- **撤掉 Initialize 反查会「暴露」原本靠两侧同时被译而侥幸自洽的查表键** — 出生管理器（`spawners_menu.dm`）的负载 `name` 既是显示名、又是 `GLOB.mob_spawners`/`GLOB.joinable_mobs` 的**查表键**；而该 GLOB 的键是在 `/obj/effect/mob_spawn/ghost_role/Initialize` 里 `. = ..()` **之后**用 `format_text(name)` 注册的。旧钩子时代父级已把 `name` 就地反查成中文 → **键是中文、P1 译过的负载也是中文，两侧同型、按钮照常工作**；显示边界化之后键回到英文、P1 仍把负载 `name` 译成中文 → `if (group_name in GLOB.mob_spawners)` 永不相等、`ui_act` 静默 `return`，表现为「点了没反应、无任何报错」，且**只有多词名坏**（单词名过不了 P1 多词门槛），极易被当成个别条目漏译。判据：`act()` 回传的那个字段的值，是不是某个 GLOB assoc 的键，而该键由 atom 的 `name` 在 Initialize 期注册。修法同「name 拼进 act 动作串」那条：另发 `id`（已在 `payload_skip_keys`）供回传，`name` 继续只做显示——**不要**把 `name` 加进 skip keys（整个菜单会退回英文）。排查同类：`grep -rn "GLOB\.[a-z_]*\[\(format_text(\)\?name" --include=*.dm code modular_nova`，其余命中多是 datum name（从不经 atom 钩子，历来如此，不属这次回退面）。
- **类型显示名表（`strings/i18n/type_vars.json`）：把「按文本倒查」换成「按类型取键」** — `nova-i18n extract` 顺带产出 `type → 目录 key`（name 31798 条 / desc 26431 条，DM 继承在 build 期用 `parent_type_without_root` 展开）。显示边界（`/atom/lang_localize_name_for_display`、`lang_localize_desc_for_display`）在 `display_name == initial(name)` 时按类型直接取键走**正向目录**，miss 才回落原有反查链。收益不是「等价替换」而是**严格更强**：没有多词门槛（单词名 4994 条、其中 4867 条早有译文，此前两条落地路径都够不到）、没有同形异义碰撞（键由类型决定）、O(1) 不经模板引擎与字面 AC。三条实现约束都是踩出来的：
  · **继承必须可截断**：子类型自己声明了 `name` 但抽不出键（非字面量/含占位符）时，继承链在该类型**中止**——沿用祖先的键会把父类型的名字挂到子类型上，而 `initial(name)` 取的是子类型自己的值。`TypeVarKeys::opaque` 守这条。
  · **表与目录必须同源**：表里的 key 由与 `emit` 完全相同的 `build_template` + `var_scope` 算出；两边算法一旦漂移，运行期是**静默永不命中**（不是报错）。`i18n_type_labels` ① 拿真表逐条断言 key 在 en 目录里存在。
  · **单测 fixture 不进目录**：`/obj/item/xxx_test` 这类声明在 `unit_tests/` 里但类型路径不在 `/datum/unit_test` 下，`suppress_aggressive` 挡不住 → 「Welding Fuel」这种纯测试串会进只增不减的目录。extract 按**声明所在文件路径**排除（`in_unit_tests`；注意 DM 的 file list 用反斜杠，`Path::components()` 认不出，要先归一）。连带后果：fixture 类型不在表里，所以行为层断言只能注入合成条目，真表只验完整性。
- **「数据不许被翻」现在是编译期门禁（`nova-i18n lint` 规则 C）** — 整条显示边界方案的地基是「实例的 name/desc 永远是 canonical English」，今天靠 rewrite 的结构（只遍历 `ty.procs`，够不到类型变量声明）保证——那是**碰巧成立**，不是设计。C1 禁止类型变量声明含 LANG；C2 的 proc 体内 `name = LANG(...)` 只放行 `NAME_LANG_ASSIGN_ALLOWLIST`（`edible.dm` 的 `"slice of [x]"`、`mail.dm` 的 `"[name] for [收件人] ([职位])"`，都是上游本就在运行期拼的复合身份名）。两个坑：
  · **AST 里没有 "LANG"**：`LANG`/`LANGU` 是 `#define`，预处理器建 AST 前已展开成 `lang_format`/`lang_format_for`，按 "LANG" 匹配永远 0 命中。
  · **判据必须是「值位置」而非「出现过」**：`name = tgui_input_text(user, LANG(提示), LANG(标题), …)` 里 LANG 只是别人的实参，赋进 name 的是玩家输入。按「出现过 LANG」判会误报（`admin_verbs.dm:387` 当场中招）。`expr_yields_lang` 只认 LANG 本身、`+` 拼接的分支、三元的分支、内插串里的内插项。
  · 门禁写完要**造一次违规验证它真的开火**（两条规则各造一处，确认报错后还原）——不会开火的门禁等于没有。
- **TGUI 负载改成「不动数据 + overlay」之后，覆盖面要靠三条各自独立的补丁撑住** — P1 不再就地改写负载值（只剩 `payload_prose_keys` 那批散文），译文改随 `json_data["i18n"]` 下发、TS 渲染期查表。回传值与服务端逐字节相同，「显示文本当标识符回传」整类静默故障结构性消失；但**从前"值到浏览器时已经是中文"顺带覆盖掉的位置**会一次性退回英文，实测三类，都补在 `catalog.ts`/`localize.ts` 而不是逐个界面改（静态扫描只认 JSX 里的 `X.prop`，经解构/中间变量到达渲染点的一概看不见，逐处改必漏）：
  · **大小写包装**（`capitalize(x)`/`toTitleCase(x)`/`capitalizeAll(x)`，约 29 处）：包装后的串与负载原值只差大小写，精确查表必 miss → overlay 额外建一张**小写归一化索引**。只对 overlay 建、不碰静态目录：overlay 的条目都是本次负载里已确认的显示值，静态目录里混着标识符形态的短键。
  · **拼进更大的串**（`` {`${x.name} - ${x.desc}`} ``、`` title={`${name} (${n})`} ``，五十余处）：整串永远不是目录键 → `substituteOverlay` 做**子串替换**。子串替换在 DM 侧（字面 AC）出过事，这里安全线是**结构性**的而非约定：overlay 的键全部经 P1 而 P1 有多词门槛（`findtext(text, " ")`），单词碎片根本进不了表；匹配面只有本次负载的几十条。再加词边界检查（前后不得是字母/数字，挡 `Water Bottle` 咬进 `Water Bottled`）与最长优先（正则交替按顺序试，短键排前面会遮住长键——DM 侧 LeftmostLongest 踩过两次）。
  · **混排 children 的碎片闸门连坐**：`localizeChildrenSegments` 原本「任一字符串 child 查不到 → 整条保持英文」。负载值现在也走这条路，于是 `["某物名", " costs ", 5, " credits"]` 会因为 `" costs "` 不是目录键而整条退回英文——而从前那个名字本来就是中文的。改成两遍：overlay 替换（永远安全、不动语序）与逐段查表（受闸门约束、全有或全无）分开，闸门中止时**回退到只做了替换的版本**，不把已替换的成果一起丢掉。这条是测试逼出来的：只在替换分支上放行不够，中止路径才是丢覆盖的地方。
  `localize.test.ts` 的「负载 overlay」一组守这三条（含词边界与最长优先的反例）。DM 侧 `i18n_payload_overlay` 守「值不动 / 散文键就地翻 / skip keys 不碰 / 不传 overlay 时行为不变」。
  仍未覆盖、需实测确认的只剩 `dangerouslySetInnerHTML`（5 处：赏金说明、证物、PDA/新闻正文）——HTML 注入 auto-localize 够不到，其中散文类走 DM 侧 `payload_prose_keys`（`description` 已在表内），玩家自写内容本就不该翻。
- **负载不再被就地改写之后，一批「补偿性」结构随之失效，要主动拆掉** — 它们都是同一个前提的产物：「P1 会把负载值译成中文，所以消费侧得把它兜回英文」。前提没了，留着就是误导后来者（每一处都写着「P1 会翻，所以…」的注释）。已拆三类：
  · **`lang_unreverse_text` 调用点 55 → 3**。32 处是负载回传补偿（货运订单/异种控制台/承包商/投票/街机/传真/死斗/化学分配器/材料成本表…），20 处是 Initialize 时代「实例名可能是中文」的补偿（区域名解析、火警器与防火门的区域前缀、摄像机、太平间、异种改名…）——后者在显示边界化那次就已经是死代码，只是当时没扫。只剩 `chat_commands.dm` 两处防御性的（Discord 命令结果，与负载无关）。**判据**：注释里写「P1/Initialize 会把它翻成中文」的，一律是死代码。
  · 货运控制台那种**手工就地反查**（`"object" = lang_reverse_text(order.pack.name)` + 消费侧 `lang_unreverse_text` 还原）是 P1 的手写版，同样整套撤掉，显示交给 overlay。
  · P1 里**「值本身是 tgui 目录键就原样返回」**那条分支连同 `i18n_tgui_strings` 整张表退休：它的职责是「把显示权交给 TS 以保住数据」，而现在数据本来就不动，跳过只会让这批值进不了 overlay。
- **P1 的多词门槛可以放开，但子串替换的多词约束必须显式搬到 TS 侧** — `findtext(text, " ")` 那道闸门唯一的理由是「别把单词标识符改坏」；负载不动数据之后，猜错的代价从「按钮哑火」退回「某处显示错」。放开后「TGUI 里单词名恒为英文」那一整类（线缆颜色、突变名、试剂/材料/设计名…）第一次有解。两条配套约束缺一不可：
  · 单词值**只走整串精确反查**，模板逆匹配与字面 AC 都跳过（那两条是给句子用的，单词过去只会徒增误伤）；
  · TS 侧 `getOverlayMatcher` 必须**只收多词键**做子串替换。从前这条由 P1 的门槛顺带保证，门槛一放开就没人守了——单词从另一个词内部开火正是 DM 侧字面 AC 那些事故的形态。`localize.test.ts` 有一条正反例守它（单词 exact 命中、但在句中不替换）。
- **`LANG` 的非文本实参从来没被本地化过（全仓 3000+ 处）** — `lang_interpolate` 里只有 `istext(arg)` 分支调 `lang_localize_arg`；而 rewrite 把 `[src]` 抬成实参时给的是 **atom 本身**（`list(src)`），于是 `"[arg]"` 插进去的是**英文名**，只能指望聊天层的字面 AC 去捞——AC 有多词门槛，单词名（floor/wrench/beaker…）永远捞不着。实测「你仔细查看The floor，但没发现什么值得注意的……」即此。顺带 `"[atom]"` 还会让 BYOND **自己补一个 "The "**（与模板里的 `\the` 无关，那条是字面宏、由 `lang_process_text_escapes` 剥），所以症状是「中文句子里嵌着 The + 英文名」。修法：`lang_interpolate` 对 atom 实参走显示边界 `lang_localize_name_for_display`——顺带拿到类型表那条快路（单词名也能翻），冠词自然消失（中文不需要）。**这条是 AGENTS.md 之前写反的地方**：旧结论说「聊天不受影响，`[src]` 早被抬成 LANG 实参、`lang_localize_arg` 没有多词门槛」——只有实参是**文本**时才成立。
- **直接取自目录的值要过一道「显示化」** — 目录存的是**源码字面形态**（`\improper`、`\n`、`\[`），这些只在编译期字面量里被引擎处理，运行期从 JSON 取回后是**字面字符**：玩家看到「\improper 太阳系精品热饮」。LANG 路径末尾早有 `lang_process_text_escapes`，但**按类型取键那条新路（`lang_type_display_text`）与精确反查的返回值都漏了**。统一走 `lang_display_value`（含反斜杠才处理，热路径零开销）。
  连带一条：剥冠词/专名宏时**必须连同后面那个空格一起吃**，否则剩「 兹克夫单元」——实测里的「那是  地板.」「你拉了拉  某物」双空格就是这个。但这条规则**不能**套到 `\s`（复数）那批：它紧贴单词、后面的空格是句子自己的（`"[n] wire\s are"` 会被吃成「wireare」）。故拆成两条正则。
- **两词碎片进字面 AC 字典 = 从单词内部开火（续）** — `lang_fallback_pattern_safe` 的停用词表原来只有冠词/介词/系动词，漏了**代词与情态动词**：目录里的碎片键 `"You can"→"你可以"` 于是进了 AC 字典，把 `You can't stop me, Owl!` 咬成「你可以't stop me, Owl!」。判据：**没有句末标点的 ≤3 词短语**，首尾任一是虚词就拒收；代词/情态动词/疑问词都得在表里。整句（有句末标点）不受影响——它们不会成为更长单词的一部分。
  排查同类的手法：这种 bug 只在「整串精确反查 miss、于是轮到 AC」的路径上暴露，所以同一句话在**聊天里正常、在其它渲染路径乱码**（实测：说话内容在聊天框正确、在头顶气泡里被咬）。`i18n_display_leaks` 拿真形态守这三条。
- **运行期把区域名拼进 `name` 的那批，要在显示边界按段拆开翻** — APC 的 `"\improper [区域名] APC"`、空气警报的 `"[区域名] Air Alarm"`、火警器与防火门的 `"[区域名] [类型名] [id_tag]"`：整串既不是目录键、又常以**单词**结尾（`APC`），精确反查与字面 AC 双双够不着 → 实测「Courtroom APC」「Brig 空气警报」（而同一局里另一处显示「拘留室 火警报警器」，因为那条路的区域名恰好命中——**同类不同待遇正是判据**）。`lang_localize_area_prefixed_name` 按三段各自的译法拆：区域名走**它自己的**显示边界（区域也是 atom，类型表与 `_map_names` 都在那条路上）、类型名优先按本类型取键（单词只有类型表够得着）、`id_tag` 尾巴原样保留。只认**前缀**形态（`findtext(...) == 1`），出现在名字中间的区域名多半是巧合。
- **MT 会把「源串本身就是中英对照」的那批再翻一遍** — 新人软管制那类模块刻意写成 `中文<br>English`，MT 看到英文半句就补译，产出「限制已解除。限制已解除。」。判据：**en 目录里的源值含 CJK**。这类的正确状态是「译文 == 源串」，且要登记进 `tools/i18n/mt/keep-english.zh-Hans.json`，否则每轮 MT 都会重新翻一次。
- **三条落地链合一（`lang_localize_chain`）+ 反查归一表（`lang_normalize_lookup`）** — 从前聊天 / TGUI 负载（P1）/ 显示边界各写了一遍「整串精确 → 模板逆匹配 → 字面 AC」，建表侧还各自挂着四条「变体键」登记。两处结构性代价，都在这次实测里现了形：
  · **顺序只在一处修对过**。「整串精确必须排在模板之前」那条当年只修了聊天链。
  · **各写一份精确查表 = 新加的表只有一条链认得**。归一表刚落地时聊天链仍在手写 `GLOB.i18n_reverse[locale][trimmed]`，于是剥标签那类键在聊天路径上永远查不到（`i18n_html_tag_keys` 当场抓住）。同样的坑在 `lang_inline_run_lookup` 里又出现一次——它收的是 exact 表本身。**判据：任何直接索引 `GLOB.i18n_reverse[...]` 的地方都是下一个漏点，一律改走 `lang_reverse_text_in`。**
  三个调用点现在只传 scope：聊天 `I18N_AC_FULL`、P1 `I18N_AC_PROSE`（单词值连模板都不过）、显示边界 `I18N_AC_NONE`。
- **显示边界不再过字面 AC** — 名字要么整串命中、要么是「区域名 + 类型名」那种可拆的合成名（分段精确翻），而 AC 是无词边界的子串替换，在这么短的串上只有误伤（「You can」咬进「You can't」的名字版）。这是 AC 收窄的第一步：它的剩余职责只有聊天里「运行期拼接、rewrite 够不到」的散文。
- **变体键 → 归一化：新增形态只改一个函数，且形态之间可组合** — 旧的四条变体（剥文法宏 / 去源码转义 / 剥标签 / 首字母大写）各自独立登记，**组合形态谁都没登记**（一个既带 `\improper` 又被 `capitalize()` 的名字就漏）。现在键与查询共用 `lang_normalize_lookup`（剥宏 → 去转义 → 折叠空白 → trim → 去成对单引号 → 多词才做首字母小写），归一结果进 `i18n_reverse_norm`。两条不能并进去的例外要留着：**剥标签**只对键侧做（查询侧是切块后的裸文本），**span 外壳**要剥了查、命中再包回去（并进归一化就把配色吃掉了）。
  实现坑：归一化后与原串相同就短路 —— **不能这么写**。归一表里有一类键本身就是归一化产物（剥标签形态），查询侧是裸句、归一化对它是恒等变换，短路等于那类永远查不到。
- **偏好菜单 asset 也走 overlay，`pref_desc_keys` 白名单退休** — `/datum/asset/json/preferences` 是**单独 fetch** 的静态资源（前端 `resolveAsset('preferences.json')`），不经 `get_payload`，所以 TGUI 负载那条路够不到它；从前专为它写了一张手写白名单（只翻各种 `description`）+ 就地改写。现在复用同一套机制：`lang_reverse_tree(data, null, overlay)` 把译文收进 `data["i18n"]`，前端 fetch 到之后 `mergeCatalogOverlay`。覆盖面从「description 一类」扩到全部显示串（name/职业名/怪癖名…），而且**值不动**——那些既是显示又是标识符的字段（`name`/`choices`/`department`）天然安全。
- **`nova-i18n lint` 规则 D（裸英文基线）** — 扫「已进 en 目录、源码里却仍是裸字面量」的句子：那说明抽取器认得它、译文多半也在，唯一缺的是 rewrite 不认那个 sink（上游把消息挪进 `deny(source, user, msg)` 那类自定义 proc 即此，现有规则一条都查不出——悬空 key 查的是反方向）。全仓 2494 条，绝大多数是**有意**留给反查路径的（`speak` 词池 / examine 累加器 / desc 变量），所以按基线增量用：`tools/i18n/bare-english-baseline.txt` 冻结现状，只对新增失败。
  规则同时比对**插值串的模板形态**（`build_template`）：`speak("[mode] level [threat] scumbag [name] in [area].")` 的模板早就在目录里、也译好了，只看纯字面量会把这一整类漏掉（beepsky 的逮捕播报就这么躺了很久）。带上插值维度后基线从 2494 涨到 4515，新增的 2135 条几乎全是 examine 累加器与公告模板那类**有意**留给模板引擎的形状——所以它只能当**增量门禁**用，绝不能当「待修清单」。
  实现坑：基线行**不能 trim**——这条规则的键是句子原文，行首空格是内容的一部分（`" Administrators have been informed."` 这类拼句片段），trim 掉会让 140 条每次都报成新增。
- **拿生产日志当语料，能把「AC 还值不值得」从猜变成量** — 没有玩家的本地局采不到数据（examine/UI 都要人去触发），但 TGS 的 round 日志里 `GAME-SAY`/`GAME-EMOTE` 记的是**本地化之前**的原始运行期串（`log_say` 与聊天渲染是两条路），且用 `*no key*` 一过滤就只剩 NPC/机器发的**目录内容**（玩家自己打的字本就不该翻）。生产服 40 局 → 24894 行 → 去重 1029 条，其中 533 条在生成时已是中文（LANG 路径正常），496 条纯英文进了聊天层。拿这 496 条过真链路：**整串精确/模板 298 条（60%）、只有 AC 能翻 22 条（4.4%）、三层都翻不了 176 条（35%）**。
  · **AC 那 4.4% 的质量是负的**：`Arresting level 5 scumbag +Yan+ in the 前部中央主通道.`、`I'll take a 培根汉堡` —— 只换掉句中的名词，产出「中文碎片嵌在英文句里」，比全英文更难看。这类句子本身是运行期拼的，正解是把**句子模板**接进 LANG，不是靠 AC 换名词。
  · 剩下 176 条里 266→有 266 条根本不在目录（动物拟声词那类本就不必翻，机器人播报那类是 rewrite 漏了 sink）。
- **`speak` 类 sink 要按「多词闸门」注册，不能整体放开** — 机器人播报（beepsky 逮捕通报、自定义售货机、mulebot…）走 `/mob/living/basic/bot/proc/speak(message)`，而 `speak` 是常见 proc 名：`/datum/brain_trauma/special/godwoken/proc/speak(type)` 收的是 `"unstun"`/`"heal"` 这种 **switch 键**，整体注册会当场把它改写成 LANG、逻辑当场坏。`rewrite.rs` 的 `is_wordy_sink` 给这类 sink 加一道多词闸门（去占位符后须含空格），单 token 实参一律不动 —— 与 LANG 实参、AC 字典的多词安全线同源。
- **顺序陷阱：必须 extract 在前、rewrite 在后** — 反过来跑会产出**悬空 key**：rewrite 先把字面量换成 `LANG(key)`，extract 再跑时源码里已经没有那条字面量，键永远发不出来（本次 `custom.dm` 两条中招）。恢复：从 rewrite 的 diff 取原文 → `nova-i18n key <ns> <tpl>` 校验哈希 → 写回 en/zh 目录。
- **examine 面可以离线枚举，不需要玩家** — examine 从不进日志，但它能被程序化产生：抽样实例化 atom → `examine(viewer)` → 过 `lang_fallback_apply_html` → 落盘离线分析。实测抽样 4155 个类型，去标签后只剩 **61 条**去重残留英文片段——这一面已经很干净，而剩下的那 61 条**每一条都指向一个具体机制缺口**，比任何静态扫描都准：
  · `a cutting tool`（命中 38 次）→ `tool_behaviour_name()` 的 switch **返回值**，被当 LANG 实参包在 `span_bold` 里显示。proc 返回值不是 sink 实参也不是类型变量，抽取器整类够不着 → 加进 `is_display_descriptor_proc`。同类还有餐厅顾客点单的 `get_order_line()`。
  · `变成 3 生肉片s` → 模板 `It can be turned into {0} {1}{2} with {3}!` 的 `{2}` 是**运行期传进来的英文复数后缀**（`amount > 1 ? "s" : ""`）。中文名词不加复数，正解是让 **zh 模板不引用该实参**（`lang_interpolate` 对模板里没出现的占位符直接忽略），而不是去改数据。
  · 制造商名（`Akhter Company Frontier Equipment` 等）是专名，保持英文正确。
- **`talk_into` 与 `speak` 同属「未注册 sink」类** — 电台播报（超物质分层警报 `CRYSTAL DELAMINATION IMMINENT! Integrity: [x]%`、AI 播报、机器人电台）走 `talk_into(speaker, message, channel)`，消息在 **[1]**。它和 `speak` 一样要过多词闸门（proc 名常见）。判据仍是那条：**同一句话在聊天里正常、在别处英文**，或「目录里有模板、源码里是裸插值串」。
- **叫声/拟声词的三个来源，各自要专门收** — 生产日志里 92 条没进目录的拟声词，全出在抽取器的三个盲区，而它们**都过不了激进 pass 的整句闸门**（单词感叹词），所以只能按形态专门收：
  · `blackboard = list(BB_EMOTE_SAY = list("Borf!", "Bork!"))` —— AI 黑板的发声词池是 list 字面量里的**关联项**，而 `is_speech_pool` 只认类型变量名（`speak`/`emote_hear`/`emote_see`）。`BB_EMOTE_*` 是 `#define`，AST 里已展开成 `"emote_say"` 等字符串，按这个键收（`is_speech_blackboard_key`）。全仓 31 处。
  · `animal_sounds` / `animal_sounds_alt` —— 动物面具的叫声池，23 处，加进 `is_speech_pool` 即可。
  · 剩下仍不在目录的多是**玩家自己打的字**（日志里 `*no key*` 只说明该 mob 没有 ckey，不代表内容是目录来的），本就不该翻。
  收进来之后 MT 会给出中文拟声（`Bork! → 汪！`、`Oink! → 哼！`、`buzzes. → 嗡嗡作响。`），与 emote 的处理一致。
- **「会改变说话」的效果在中文下整类空转，且分三种结构** — 口吃/脑损/焦虑、蜥蜴嘶声、苍蝇嗡嗡、各种语音基因突变，全都是**字母级**变换，中文文本里既没有词间空格也没有拉丁字母，于是玩家看到的蜥蜴人和常人说话一模一样。三种结构各有各的失效点，缺一处修都不完整：
  · **逐词切分**（`/datum/status_effect/speech/handle_message` 按 `" "` 切、再按 `" "` 拼回）：中文整句 = 一个「词」，一句话只在句首触发一次。改为按**字**切、空串拼回，并抽 1/4 的字参与——效果自带的每词概率（口吃 75%）对单字而言太密，每个字都口吃就没法读了。
  · **口吃正则**（`^(…)\b([^aeoiuh\d]h|qu|[^\d])(.*)`）：靠 `\b` 与拉丁字母定位首字母，单个汉字匹配不上 → 原样返回，等于完全不触发。判据用「不含 ASCII 字母」改走整字重复（「我-我-我」），避免误伤中英混排里的英文词。
  · **拟声替换表**（`speechmod` 组件的 `replacements`）：`s→sss` / `s→z` 在中文上零命中。仓库里已有**俄语版先例**（同表加西里尔映射），中文照此加一张，但**不能硬换字**（中文没有对应音字母，换字会毁词义）→ 改成**标点锚定**：句末补一声「嘶/嗞」，已有的拟声字拉长。
  · 词表突变（chav/elvis/ork/法语…）的 key 是英文单词，中文里永不匹配，整类仍是空转；但它们的 **`end_string` 是无条件追加的**（`", mate"`、`" OLE!"`、`", bork"`），所以中文服的实际表现是「中文句子后挂一条英文尾巴」。在 `speechmod` 组件里一处收口反查（13 个调用点共用），并让 extract 收具名实参 `end_string = …`（AssignOp 的 lhs 是**标识符**而非字符串，激进 pass 的整句闸门也收不到这种短后缀）。
- **判据写成「locale != en」是错的，要写「locale 是中文」** — 伪 locale（`qps-ploc`）与将来任何第二语言都会命中前者，于是中文专用的拟声表被套到英文文本上，**上游的 `speech_modifiers` 单测当场抓住**（蜥蜴人的 `s→sss` 断言拿到了未变形的英文）。用 `lang_locale_is_chinese()`。更稳的一层：能用**文本形态**判据就别用 locale 判据——「按字切」那处改判「无词间空格且含 CJK」（`lang_contains_cjk`），中英混排、伪 locale、将来的其它语言都不会误伤。
- **语音替换词表按 locale 取表：`lang_speech_replacements()`** — chav/elvis/ork/crustacean 等 13 张表的**键是英文单词**，靠 `replacetextEx` 子串替换；中文句子里那些键永不出现 → 整类突变空转。中文表按 `<名>.<locale>.json` 与英文表**同目录**放（`chav_replacement.zh-Hans.json`），有就用、没有就退回英文表 —— 新增语言只加文件、不改调用点。两条约束：
  · **不能放 `strings/<locale>/` 子目录**：`GLOB.string_cache` 只按**文件名**索引、不含目录，同名文件会互相覆盖，谁先加载谁赢、另一边静默拿到错表。
  · 中文表的键必须是**多字词**：`replacetextEx` 无词边界，拿单字当键（「下」「是」）会在词内开火，把「下面」「不是」一起改掉 —— 与字面 AC 那些事故同形。
- **`visible_message` 形态拿不到语料，别再找了** — 生产日志类型逐个看过：`attack.log` 是日志自己的措辞（不是渲染文本）、`hallucinations.html` 只记幻觉**类型**、`paper.log` 是玩家自写。这个形态**不进任何日志**。要数据只有两条路：真实回合开 `I18N_LOG_MISSES`，或写生成器（但 visible_message 需要模拟交互，比 examine 抽样难得多）。在此之前不要拿"AC 只贡献 4.4%"去删聊天 AC —— 那个数字只覆盖 NPC say/emote 一种形态。
- **交互生成器：拿单元测试套件当驱动、`I18N_LOG_MISSES` 当采集器（`tools/i18n/miss-harvest.sh`）** — examine 面可以靠抽样实例化枚举，但 `visible_message`/balloon/交互消息**必须有人触发**，而生产日志又不记它们的渲染结果。测试套件本来就会大量创建 atom、打架、开机器、跑手术 —— 它就是现成的交互驱动，配上早就存在的漏翻采集即可。与 `pseudo-test.sh` 互补：那个用**伪 locale** 抓「标识符被反查变异 → 功能坏」，这个用**真译文**抓「玩家还能看到英文」，谁也替代不了谁。
  · 抄 pseudo-test.sh 时**必须连它那条「指定单测地图」一起抄**（`cp _maps/runtimestation_minimal.json data/next_map.json`）：多数单测是 `UNIT_TEST_DEBUG_MAP_ONLY`，不指定地图会被整批跳过，采集结果看着「没几条漏翻」其实根本没跑。
  · 首跑产出 585 条，两个高价值缺口一次现形：**`death_message`**（74 处，计数直接打满上限 —— 每次生物死亡全场可见）与 **`preview_name`**（363 处，偏好菜单逐件配装预览名）。两者躲过以往所有检查的原因相同：**过不了激进 pass 的整句闸门**（一个以小写动词开头、一个是无标点名词短语）。静态扫描看不出它们该翻，只有「玩家真的看到了英文」这个运行期事实能暴露。
  · 采集里确认**不该翻**的两类：贡献者 ckey 列表、`The creature` / `a muted` 这种拼句碎片。
- **单字符串一律不进目录** — `preview_name = "X"` 这种进目录之后，反查表会把任何恰好等于 `"X"` 的显示值改掉，而 `switch("X")` 遍地都是（lint 的碰撞规则当场以**高**级报错抓到 `admin/topic.dm`）。`emit` 按**字符数**（非字节）挡掉长度 < 2 的模板，中文单字同样挡住 —— 单个汉字的显示串也只可能是标识符或图标标签。注意目录**只增不减**：脏键一旦写进去要手工摘，光加守卫不够。
- **聊天里的名字是「独立成块」经过落地层的，整句反查够不着它** — `<span class='name'>The wolf</span> 说，「…」` 被 `lang_fallback_apply_html` 按标签切块之后，名字**自成一块**，于是整串精确反查与模板引擎都碰不到它，只剩字面 AC —— 而 AC 有多词门槛，`wolf`/`animal` 这种单词名永远捞不着，玩家看到「The wolf 说，「……」」。判据：漏翻采集里出现「片段 == 整块」的名字项（加了整行上下文才看得出来，只记片段时它完全指不出调用点）。两处已修：emote（`emotes.dm` 的 8 个 `<b>[user]</b>` 显示点）与说话/电台（`say.dm` 的 `compose_message` namepart）。**剩余面**：`span_name()` 宏的其余 48 个调用点同形态，未修；真要一次覆盖，应该在切块器里认「整块就是某个 name span 的内容」这个形状，而不是逐个改调用点。
- **名字到达显示层时已经带了冠词，不能直接喂显示边界** — `/atom/proc/get_voice()` 返回 `"[src]"`，而 BYOND 对**非专有名词**会自动补 "The"（该 proc 的注释就这么写的）。于是 `namepart` 是 `"The wolf"`，显示边界按 `name == initial(name)` 判身份名，直接拒翻 —— 静默返回原值、没有任何报错，第一版修复因此完全无效。而且说话/电台路径还会把说话者包进 `/atom/movable/virtualspeaker`，它的 `name` 本身就是带冠词的那串，按 canonical name 取译名的第二版同样够不着。
  最终判据不看类型、只看形态：**BYOND 只给非专名补冠词，而玩家角色名是专名、永远不带冠词**，所以「带冠词」本身就证明了它不是玩家身份名 —— 剥掉冠词整串精确反查即可（中文不需要冠词）。这与「按字切」那处从 locale 判据改成「无空格 + 含 CJK」是同一个思路：**中间层会不断改写值，依赖「这个值是什么类型」的判据必然被绕过；依赖「值长什么样」才稳**。
- **漏翻采集的数据是统计量，不能用来判定「某处修好了没有」** — 测试套件每轮生成的生物不同（这轮有狼、下轮有军团兵），所以「`The wolf` 从榜上消失了」完全可能只是这轮没刷出狼。我靠这个连续误判了两轮。**确定性问题要用确定性断言**：直接调 `compose_message` 断言输出里有译名、没有英文名，一轮门禁就能定论，而且这条断言会永久留在 `i18n_display_leaks` 里当回归门禁。采集的正确用途是**发现**未知缺口，不是**验证**已知修复。
- **`list -= null` 只删掉第一个 null** — DM 的 `Remove()` 每个实参只摘**一个**实例。`get_examine_name` 的中文分支把冠词槽清成 null 再 `override -= null`，而 override 里本来就有一个空的 before 槽 → 剩下那个被 `jointext(override, " ")` 连成**前导空格**，玩家看到「那是  核心模块架 'alpha'.」。上游原代码只有一个 null，所以那条 `-=` 一直是对的，是我们多清一个槽才踩响。这类「拼出来多一个空格」的症状先怀疑列表里有没剔干净的空槽，别去查文法宏剥离——两者症状一模一样（`i18n_display_leaks` ⑥ 守这条）。
- **英文的动词一致性记号（`p_do()`/`p_s()`/`p_es()`）作 LANG 实参时必须落成空串** — 中文没有这个成分，译成任何词都是多余的（「他does似乎不太在意寒冷」——模板译好了、实参漏出来）。两个坑叠在一起：① `lang_pronoun` 原来用 `pmap[word] || word` 取值，**空串在 DM 里为假**，映成 `""` 也会被当 miss 返回英文，得改成 `isnull` 判定；② 光靠「中文模板别引用该占位符」不够——那要求每条译文都记得删，漏一条就漏一次。收口在 `lang_pronoun` 里，译文怎么写都不出事。
- **LANG 实参的「裸文本 + span 包裹」混合形状，剥外壳那条分支整条 miss** — `lang_localize_arg` 剥首尾标签的正则要求整串**首尾都是标签**，而 `" and [EXAMINE_HINT(\"secured with metal cables\")]"` 多一个前导 " and " 就不匹配，于是连里面早就译好的那半句一起留成英文（法则架例检的「固定于地面 and 用金属线缆固定」）。而且**指望聊天层兜住是错的**：切块器会把这个 " and " 和它前面**模板译出来的中文**连成同一块，整块查不到、AC 又因多词门槛拒收 " and "。修法是让这类实参在**插值之前**就走一遍聊天同款切块器（`lang_fallback_apply_html`），那时 " and " 自成一块、目录里正好有它。注意这条路存在一个环（实参 → 切块器 → 模板逆匹配 → 捕获值又回 `lang_localize_arg`），每跳都更短、理论上收敛，但它跑在聊天热路径上，得显式钉深度上限（`I18N_ARG_HTML_MAX_DEPTH`）。
- **`speak_emote`（说话动词池）整类没抽** — `speak_emote = list("brays")` 决定聊天里那句「XX brays, "……"」的动词，落地走 `say.dm` 的 `lang_reverse_text(say_mod)`，是**整串精确**反查，所以单词条目在这里安全（不进字面 AC 的多词字典）。补进 `is_speech_pool` 后一次收进 83 条（brays/moos/purrs/beeps softly…），零标识符混入。判据：聊天里说话动词是英文、而说的内容是中文。
- **切块器里认形状，比逐个改调用点省事得多** — `span_name()` 有 54 个调用点，逐个改既走不完又挡不住新增。名字块的特征在**切块器**这一层是显式的：上一个发出的标签是 `<span class='name'>`。在那里把整块交给「只做整串精确反查 + 剥冠词」的专用 proc（不过模板、不过 AC——这么短的串上子串替换只有误伤），一处收口覆盖全部调用点。同理适用于任何「有稳定包裹标签」的显示类别。
- **编译器的 `unreachable_pattern` 警告是真 bug 的告示牌** — `lint.rs` 的 `Term::InterpString(..)` 那支只记模板就结束，后面另有一条同名 arm 想下探内插表达式里的字面量，**永不可达**（警告一直在报，一直没人看）。于是 `to_chat(user, "[icon2html(…)][span_notice(\"Analyzing reagents...\")]")` 这类嵌套字面量整类不在规则 D 视野里。合并两支后基线新增 224 条——全是「靠聊天层整串精确反查落地」的正常形状，收进基线即可；真正的价值是这道门以后看得见这一类了。
- **MT 的「单个全大写词一律当缩写」把拟声/呐喊整类挡在待译队列外** — `hasTranslatableEnglish` 早先修过一次（多词全大写句该译），但**单词**形态仍旧一刀切成缩写，于是 `HONK!!`、`AUUUUUU`、`EHEHEHEHEH`、`HISSSSS`、`OBJECTIVES:` 从没进过队列，玩家在聊天里看到的就是一串英文大写字母。判据只能用**形态**、不能猜词义：句末标点（缩写不带 `!`/`?`/`:`）或同一字母连出 3 个以上（拟声的特征），且**首字符必须是字母**（`'s HP:` 这种所有格碎片同样以 `:` 收尾，翻了只会在句中拼出病句）。纯字母短缩写（APC/RCD/ODST/RGB）两条都不满足，仍旧不译。剩下的 `LOCKED`/`WARNING`/`HALT`/`<b><u>ARMOR (I-X)</u></b>` 这类无标点全大写标签只能手工补——形态上与缩写无法区分。
- **assoc 的键是文案还是标识符，只能按 proc 语境定，别复用 walker 时连语义一起复用** — `examine_tags` 是全仓唯一「键即文案」的形状；而配装项的 `get_item_information()` 恰好相反：`.[FA_ICON_HAT_COWBOY] = "Top of Head"` 的**键是图标标识符、值才是玩家可见标签**。共用一个 walker 时必须能关掉抽键那一支，否则 `FA_ICON_*` 宏展开出来的 `"fa-hat-cowboy"` 会被抽进目录、进而毒化反查表。
- **运行期在类型名两侧加缀的实例名（obj 版）** — `name = "\proper [name] '[designation]'"`（AI 法则架）、贴标机的 `"[name] (标签)"`：整串既不是目录键、类型表按 `initial(name)` 也对不上 → 精确反查与类型表双双 miss，只剩聊天层的字面 AC（多词才走、无词边界，还会把 `\proper` 标记字节留给玩家）。判据一条：**剥掉文法宏之后 `initial(name)` 逐字节是它的前缀**，前缀走类型表（单词名也能翻）、其余原样保留。mob 侧另有一条更严的同类规则（只放行 `" (…)"` 后缀）——那里 name 是身份不能放宽，obj/turf 的 name 从来只是类型标签，按前缀拆是安全的。
- **漏翻采集器的闸门必须按「传进来的是什么」分两套** — 旧采集只有两个埋点（`lang_fallback_apply` 出口、P1 的 miss 分支），它们收的是**整行渲染文本**（混着已译中文、玩家打的字、人名、数字），所以必须先抽「连续拉丁词 run」再按多词门槛过滤，否则日志被人名淹掉。代价是**单词漏翻整类结构性看不见**——而显示边界的 `wrench`/`beaker`、LANG 实参里的状态词恰恰全是单词，于是「采集结果很干净」是假象。`lang_log_miss_value` 收的是**一个语义单元的完整值**（一个 name、一个 LANG 实参、一个模板捕获值），调用点已经确定了「这就是该翻的显示文本」→ 不抽 run、不设多词门槛，只挡形态上不可能是文案的（含 `_`/`/`、全大写无空格、含 CJK）。埋点：`lang_localize_arg`（arg）、`lang_localize_display_name`（display，**带 `src.type`**）、`lang_localize_desc_for_display`（desc）、`lang_tpl_localize_arg`（capture）、`lang_localize_name_chunk`（namespan）。一轮实测新增 228 条，全是旧采集看不见的那一类。
- **日志行里的上下文，离线端不切掉就等于整份报告分类全错** — 「首次记录带整行上下文」写成 `n=1 src=X | text || 整行: …`，而 `miss-scan.mjs` 的 `LINE_RE` 把 `\| (.*)$` 整段当 text → 带着上下文去查目录永远对不上，全落进「没进目录」桶。**绝大多数串只出现一次**，所以这不是边角误差，是每份报告都错。加上下文和消费上下文必须同一次改完。
- **TGUI 前端是 DM 侧完全看不见的一面** — 静态 JSX 文本、可翻 prop、children 模板都在浏览器里查表，查不到就原样渲染，服务端毫无痕迹；P1 那条只覆盖**负载值**（服务端能看到的那半边）。`i18n/missLog.ts` 经 `Byond.sendMessage({type:'i18n/miss'})` 回传（开关由 `config.i18nLogMisses` 下发，默认关、去重、攒批）。**判据陷阱**：`tgui-ui` 这一栏在 miss-harvest 里恒为 0，那是「单测套件没有浏览器」而不是「前端没有漏翻」——这一面只有真人开界面才产得出数据。
- **`balloon_alert` 是唯一不经任何落地层的常规显示面** — to_chat / browse / statpanel / 公告 / 状态栏各自都接了 `lang_fallback_apply`，气泡的 maptext 直接送客户端。绝大多数调用点已被 rewrite 改成 LANG，任何漏网的都会**无声显英文且采集器也看不到**。接上落地层要放在 `MeasureText` **之前**（气泡宽高按那串量）。
- **「局部变量一跳」：赋给后来当作 LANG 实参的局部变量的字面量，从前一条都进不了目录** — `var/subject` + `switch` 里几个 `subject = "the left side of your body"` + `LANG(key, list(subject))`：模板抽到了也译了，那几个字面量既不是 sink 实参、也不是类型变量、更不是 LANG 实参子树里的字面量（子树里只有标识符）→ 玩家看到中文句子里嵌着整段英文。局部累加器（`suffixes += " Targetable by contractors."`）同形。修法是 proc 级两趟扫描：先收「在 LANG 实参位置出现过的**裸**标识符」（`x.y` 不收，赋值点不在本 proc），再收赋给它们的字面量，闸门复用 `is_lang_arg_text`。全仓一次收进 110 条、零标识符混入。**变量名不可枚举是这条的关键**——实测命中里有 `HOW_big_of_a_miss_did_we_just_make`，任何白名单都抓不到。
  连带产生 4 条 `值兼标识符` 碰撞告警（`"ice cream"`/`"General Color"` 这类 `#define` 出来的 switch 键同时进了 LANG 实参）。它们**是安全的**：变量只从 define 赋值、只与 define 比较，译文只在 `lang_localize_arg` 的输出上产生，从不写回数据——这正是「负载不动数据」那套架构买到的东西。
- **英文复数实参要回退到单数** — 物种的 `plural_form = "[name]\s"`（`Voxs`/`Akulae`）与各处 `"[x]s"` 是运行期拼的，整串永远不是目录键，而单数早在目录里。中文没有复数，去词尾再精确查一次即可。安全线是「**单数必须整串精确命中目录**」——碰不上就原样返回，不做任何形态猜测。显式写出来的 `plural_form = "Flypeople"` 另走 SINK_VARS。
- **中文拟声/替换表必须叠加在英文表之上，不能替换** — 两张表是**字母级**与**汉字级**两套互不相干的规则（`s→sss` 只在拉丁字母上开火、`。→嘶。` 只在中文标点上开火）。写成「中文服就整张换掉」有两个代价，第二个是硬伤：中文服上的英文发言（玩家打英文、机器播报）丢掉效果；**上游 `speech_modifiers` 单测在真 zh-Hans 下直接红**（它断言的正是英文输入 "She is so sassy" 的变形结果）——而伪 locale 门禁抓不到这条，只有 `miss-harvest.sh`（跑真 locale）会暴露。叠加时半角标点那几条要**按前置汉字锚定**（`([一-鿿])\.`），否则裸的 `"."` 会在英文句子上开火。`lang_merge_speech_replacements` 收口；**不能用 `base + extra`**：DM 的 list `+` 对关联列表只并键不带值。
- **漏翻采集器不设条数上限** — 它是采集**工具**、不是运行期功能（`I18N_LOG_MISSES` 默认关，只在 miss-harvest 或专门开一局采数据时打开），所以「防内存膨胀」那个理由不成立，而截断的代价是实打实的：唯一后果是「条数不再增长」，**与「真的采干净了」表现完全一样**，会让后续所有分析静默失真。去重之后总量天然有界（等于「场上有多少条不同的英文」，一轮几百到几千条）。同理适用于前端那一侧。这类「为了安全加的上限」在采集/审计工具里一律要问一句：打满时看得出来吗？看不出来就别加。
- **`load_strings_file` 对 strings/ 数据做**就地**反查，所以拿 `strings()` 的**值**做断言必然不稳** — 非英文 locale 下加载 `chav_replacement.json` 会当场把它的值换成中文，而 `GLOB.string_cache` 是全局的、一局只加载一次。于是「英文 locale 下不该取到中文表」这条断言实际在断言「这个文件是在切 locale 之前还是之后第一次被读的」，跑真 zh-Hans 必红。**判据要看键**——键从不被反查碰。这条在伪 locale 门禁下是**假绿**（qps-ploc 把值包成 `⟦…⟧`，不含 CJK，断言照样过），只有 `miss-harvest.sh`（跑真译文）会暴露：又一次印证两个门禁不能互相替代。
- **「剥冠词再精确查一次」属于共用链，不该在各落地点各写一遍** — BYOND 对非专名自动补 "The"/"a"，名字到达落地层时是 `The mi-go`、`a wrench`，整串不是目录键。从前 `lang_localize_arg` 和聊天的 name-span 分支各自实现了这件事，于是「名字独立成块、但包在 name span 以外的标签里」（`<b>[src]</b>` 那类）整块落不了地——实测 `The mi-go`/`The alien runner` 各 100 次打满计数。**判据是漏翻记录里「片段与整块逐字节相同」**（这正是「首次记录带整行」那条改动买到的东西）。并进 `lang_localize_chain`（整串 miss 之后、模板之前）之后 fallback 面直接降了 35%；它要求余下部分**整串精确命中目录**，比模板逆匹配和字面 AC 都保守，不会抢走更具体的证据。与其继续猜是哪个标签，不如把规则放进链里。
- **MT 的 `stripNoise` 把 kebab-case 当标识符剥掉，专有名词会因此永远不进待译队列** — `\b[a-z][a-z0-9]*(?:-[a-z0-9]+)+\b` 挡掉 `walking-aid`/`quick-equip`/`body-l-leg` 是对的，但同一条也挡掉了 `mi-go` 这种**连字符专名**：它在目录里、有 scope、是个 mob 的 name，却永远 zh==en。症状是漏翻采集里反复出现且分类为「在目录未译」。更彻底的判据是查 `scopes.json`——键的 scope 是 mob/obj/turf 类型路径就说明它是 name/desc，形态再像标识符也是显示文本；在那之前这类只能手工补。
- **`miss-harvest.sh` 也要还原 `icons/map_icons/`** — 禁掉 rustg iconforge 之后地图预览图标改由 DM 回退生成，像素与已提交 `.dmi` 有差。`pseudo-test.sh` 早就在还原，harvest 漏了，于是每跑一次就有 20 个 `.dmi` 混进工作区、极易被当成本次改动一起提交。
- **「局部变量一跳」的准入面必须限死在「本来就会进目录的显示模板」上** — 把它从「LANG 实参」放宽到「proc 里任何插值串的槽」，一次抽取就混进单测断言串（`SSShe isss ssso sssasssy`、`The quick brown fox…`）、单位格式化的期望值（`535 mA`/`1.234 kPa`）、管理面板的 HTML 骨架、`callback.object is null.` 这类调试串 —— 218 条里大半是垃圾，而**目录只增不减**，脏键进去就永久留着（改抽取规则前先 `git checkout strings/i18n/en`）。收窄成两种形状之后是 41 条、零噪音：① 插值串是**已注册 sink 的实参**（`sink_message_args`）；② 插值串被赋给一个**与 SINK_VARS 同名**的变量（`gain_text = span_warning("You can't feel [subject] anymore!")`）。判据的道理：这两种形状正是「整句会被抽成模板进目录」的形状，而运行期模板引擎逆匹配捕获到的就是槽里那个值——只有它们需要槽里的字面量也在目录里。
- **`in_unit_tests` 只挡了类型变量那条路，proc 体的每一条新规则都要自己挡一遍** — step 1 按「声明所在文件」排除了 `unit_tests/`，step 2（proc 体）没有；`suppress_aggressive` 又只按类型路径 `/datum/unit_test` 判，够不着 fixture 类型。新加的 proc 体规则要显式接上 `suppress_aggressive || in_unit_tests(proc_value.location)`，否则测试夹具串会永久写进目录。
- **TGUI 前端漏翻可以离线扫掠，不需要 BYOND 也不需要真人（`tools/i18n/tgui-sweep.sh`）** — `missLog.ts` 那条运行期回传只有真人开界面才产得出数据，`miss-harvest.sh` 跑一整轮单测 `tgui-ui` 恒为 0，那是「测试套件没有浏览器」不是「前端没有漏翻」。仓库里已有 happy-dom + `@testing-library/react`（`Radio.test.tsx` 在用），把 329 个界面挨个挂上去即可。四个坑，每个都会让结果**看着干净其实什么都没测**：
  · **自动本地化只在 rspack 里接着**（`swcReactOptions('tgui/i18n')` 把 jsxImportSource 指向 i18n 目录的 jsx-runtime）。`bun test` 用原生 `react/jsx-runtime`，整条链根本不跑 —— 第一版「渲染成功 329、漏翻 0 条」就是这么来的。用 `mock.module` 在测试环境里复现同一件事；**必须先把真函数取成局部常量**，`mock.module` 的工厂是惰性求值的，工厂里再读命名空间对象时它已经指向 mock 自己 → 每个界面都是 `Maximum call stack size exceeded`。
  · **空数据下绝大多数界面会崩**，一崩整棵树都渲染不出来。按界面自己的负载类型用 tsc API 合成假数据：判据取 **`useBackend<T>()` 的类型实参**（按「名字以 Data 结尾的 type alias」找会漏掉 `type Info` 那批，整批反派介绍界面起不来）；数组给**一个元素**（空数组等于列表行整类渲染不到，而行里文案最多）；字符串字面量联合要给**真实的那个值**（否则 switch 全落空）。合成的普通字符串用**生造词**——真实英文词会命中目录，把「这处没翻」测成「翻了」。
  · **攒批要按界面收口**：靠 `missLog` 的定时器的话所有条目在扫掠末尾一次性发出，来源标签全变成最后那个界面 —— 而「哪个界面」正是这份报告唯一可行动的信息。
  · **扫掠自身的产物要过滤**（合成串、mock 的窗口标题），否则它们混在结果里像是漏翻。
- **`miss-scan.mjs` 的 `src=(\w+)` 匹配不了带连字符的来源名** — `tgui-ui` 整类被静默丢掉，而症状不是「报 0 条」而是「一行都不解析」（脚本直接说「没有解析到任何 miss 行」）。加来源名时记得同步这条正则。
- **售货机物品名这类「TGUI 里的 atom 名」早就通了，剩的是 MT 的形态误伤** — 实测 `/obj/item` 有 name 键的 17170 个类型里已译 17044（99.3%），售货机那条路（`collect_records_for_static_data` 里 `name = lang_reverse_text(record.name)`）也是通的。剩下 126 条里最大的一类是 **MT 的 `stripNoise` 把 kebab-case 当标识符剥掉**：`bowler-hat`/`top-hat`/`flip-flops`/`mi-go` 在目录里、有 scope、却永远 `zh==en`。用 `scopes.json` 判「这是不是 atom 的 name/desc」来放行，但**两道闸门缺一不可**（都是实测逼出来的）：
  · **不能只看 `#name`**：`/datum/verb_metadata/client/body_groin#name` 是 verb 标识符、`/datum/asset/spritesheet/…#name` 是资源 id —— 46 条 kebab 未译里 29 条是这种。判据要加「类型路径是 `/obj`|`/mob`|`/turf`|`/area`」。
  · **atom 上的蛇形值照样是标识符**，而且要排除 `/obj/effect/*`（临时视觉、地标、抽象持有物按构造不可见）。放开一次就把 `to_suit → 进入护甲`、`held_tk_effect → 念动持有效果` 全翻了，`bird_1 → 一号停机位`（导航信标编号）还是错的 —— 而这些词会进**全局反查表**。撤了 24 条。
  加完闸门后新译 40 条真显示名（食物/衣物/器官/海报），另有一批专名（`M1911`/`pAI`/`Pun Pun`/`DS-2`）被模型判成保持英文并写进 `keep-english`，正是该有的结果。
- **「新造单词词对一律退回英文」这条太严了，正确的闸门是两道门禁而不是词数** — 真正的风险从来不是「单词」，是**该词在 DM 里也被当标识符用**（线缆颜色 `blue`/`crimson` 那次事故的形态）。而这件事有两道现成的、可证伪的门禁：`nova-i18n lint` 的碰撞规则（扫 DM 源码里的 `==`/`switch`/下标）与 `i18n_real_catalog` 的域内表断言（钉死史莱姆/线缆颜色**不应**进全局反查表）。拿这条规则先后撤掉的 13 个词（`Fall`/`Land`/`Walls`/`Clone`/`Reprogram`/`Orbiters`…）做了一次决定性实验：**全部译回去之后两道门禁零命中**，`i18n_real_catalog` 与全量伪 locale 单测照常全绿。所以判据改成：新增单词键**先译，再跑这两道门禁**；碰撞报出来的才退回英文并登记 keep-english。词数只是启发式，门禁才是证据。
- **扫掠对下拉框是覆盖的，收起状态也算** — `localizeDropdownProps` 在 **prop 阶段**就跑（`localizeProps` 对每个 JSX 元素调用），不需要点开菜单；实测渲染一个收起的 `<Dropdown options={[…]}/>`，未命中的选项照样被记进 miss。真正收不到的是**要交互才渲染**的内容：模态框、未选中页签的正文、`Collapsible` 的展开体。那一面只有真人开界面时 `missLog.ts` 的运行期回传够得着 —— 两条通道是互补的，别拿扫掠通过当「前端全译了」。
- **扫掠的「没进目录」不是待办清单，里面混着不该翻的** — `CodexGigas` 一个界面贡献 29 条，全是**恶魔名生成器的音节表**（`TITLES`/`NAMES`/`SUFFIXES`：`Lord`/`Prelate`/`hal`/`ve`/`odr`），翻了就毁掉名字生成。`Lord` 同时出现在「已译未接通」（它在 `strings.json` 里有译文）恰好是反证：**那条「已译未接通」不是缺口，是警告**——一旦有人把它桥进 `tgui.json`，生成器就会吐出中英混排的名字。同类还有 `track`/`Disposals` 这种跨命名空间同形词。处理这份报告要先按「这串到底该不该翻」过一遍，别照单全收。
- **扫掠自己的合成数据会造出假阳性** — 界面把负载值拼进显示文本（`${moles} Moles`、`${x} units of ${y}`）时，喂进去的是合成值，拼出来的整串当然不在目录里；但真实对局里那串**也不会**在目录里，它本来就该走「模板 + 运行期值」那条路。`null`/`undefined`/`NaN` 是这类的指纹，按它过滤掉一批（248 → 221）。
- **`CONSTANT_LABEL_TABLES` 收进来的单词键，译文不能现编** — `tgui.json` 会被 `build_i18n_cache` 扫进 **DM 侧全局反查表**，凭空多出的单词词对就是扩大全局误翻面（线缆颜色那次踩过）。中心指挥投送舱的 37 条标签里有 14 个单词键，MT 现编了 10 条（`Fall→坠落`、`Land→着陆`、`Walls→墙壁`…）—— 按既有规矩全部退回英文并登记进 keep-english，只保留「译文沿用其它命名空间既有词对」的那 3 条（`Exit`/`Gib`/`Mobs`）与全部多词键。代价是界面上几个单词标签仍是英文，这是那条安全线的既定取舍。**审计手法**：拿新增键逐条比对「该 en→zh 是否已存在于其它命名空间」。
- **运行期改名的 `name = "字面量"`（proc 体内）整类没抽** — SINK_VARS 只看**类型变量声明**，够不着 `switch(caste)` 里按分支改名的那批（`if(ALIEN_RAVAGER) name = "alien ravager"`）。实例名一旦偏离 `initial(name)`，显示边界的类型表也对不上（那张表按 `initial(name)` 取键）→ 只剩整串反查，而这串从没进过目录 —— 实测 `The alien ravager` 计数直接打满。抽取时按两道闸门放行：**多词**（`name = "Show"`/`"Hide"`/`"Zip"` 那批是 UI 开关词与标识符）+ **类型是 atom**（`/obj`|`/mob`|`/turf`|`/area`；`/datum/...#name` 里 verb 元数据与资源 id 浓度很高，与 MT 那条 atom-scope 判据同源）。全仓 172 条，全是异形各形态/家具/组装件这类真显示名。
- **`propTemplate` 只认「属性值本身就是模板字面量」，三元或 `&&` 包一层就整条不抽** — `tooltip={min ? \`Min (${min})\` : 'Min'}` 里 `templateParts` 对 ConditionalExpression 直接返回一个纯占位符（没有 text 段）→ `propTemplate` 返回 null，sidecar 里根本没有 `Min ({0})`。两条出路：改成 children 模板（`tooltip` 收 ReactNode 时可行），或让 `propTemplate` 下探三元两支。
- **`LabeledList.Item` 只对 string 型 label 自动补冒号** — 把 label 改成 children 模板（ReactNode）之后那个冒号会消失，必须显式写进模板里。这也解释了为什么漏翻采集里 `Move 1` 与 `Move 1:` 是同一处的两条记录。
- **单位符号与坐标轴字母不该翻，别为了消灭报告条目硬改** — `K`/`kPa`/`u`/`x`/`m`/`pH`/`X`/`Y`/`0h 0m 0s` 中文照写。它们也过不了 `propTemplate` 的锚门槛（字面段 trim 后须 ≥5 字符且含 `[A-Za-z]{3}`）与 `childrenTemplate` 的 `[A-Za-z]{2}` 门槛 —— **闸门挡住它们是对的**，放宽闸门去收纯符号骨架反而会把误翻面打开。同类判据：`x{0}`（数量按钮）中文本来就写 `x1`。
- **报告里「同形不同待遇」永远是判据** — `Buy`/`Refund` 按钮是中文而 `Learn` 是英文，说明那三个都是 DM 负载值（`Buywords` 枚举）、渲染形状本来就对，缺的只是 `Learn` 不在前端目录里；改 JSX 无济于事。同理 `Health` 页签是中文而 `Orbiters` 是英文 → `Orbit/constants.ts` 的 `VIEWMODE` **键**没被抽（值是图标名，绝不能收；现有 `CONSTANT_LABEL_TABLES` 没有「只收键」这一档）。
- **`lang_fallback_setup` 把「目录还没就绪」缓存成了 `"none"` —— 整局字面 AC 永久无声关闭** — `lang_build_reverse` 在 `i18n_cache` 未就绪时特意「返回空表但**不缓存**」（免得把空反查表钉死），可 `lang_fallback_setup` 拿到那张空表会一路走到底、把 `GLOB.i18n_fallback_state[locale]` 钉成 `"none"`，此后一律返回 FALSE。同一个「别把未就绪状态缓存起来」的道理，上一层做了、这一层漏了。现在未就绪时**直接返回 FALSE 且不写 state**，留待下次重试（`i18n_real_catalog` ⑥d 模拟那一刻守着）。
  这条是**排除法**找出来的，值得记住那个过程：漏翻日志里「基础句已是中文、后缀整段英文」的那一行，我按三种形态各写了一条真目录断言（后缀单独喂、英文基础句 + 后缀、日志里逐字节的中英混排行）——**三条全过**。落地层在任何可构造的形态下都对，那问题就只能在「什么时候建的表」而不是「表里有什么」。**先用断言把「不是什么」钉死，剩下的那一格就是答案**；三条断言本身也就永久留在门禁里了。
- **`propTemplate` 只认「属性值本身就是模板字面量」，三元/`||`/`&&` 包一层就整条不抽** — `tooltip={min ? \`Min (${min})\` : 'Min'}` 里 `templateParts` 对 ConditionalExpression 直接返回一个纯占位符（没有 text 段）→ `propTemplate` 返回 null，sidecar 里根本没有 `Min ({0})`。三元的每一支在运行期都是独立的成品串，`propTemplateBranches` 把它们拆开各自当模板收；每支仍要各自过原有那三道安全线，准入面不放宽。全仓 39 条。**审计**（拿目录里所有非模板 key 逐条过逆匹配）：4 条命中，全部良性 —— 被吃的四句本身就是目录键，而整串精确查表永远排在逆匹配之前。
- **第三人称战斗消息（全场可见）漏了 LANG，而 victim/attacker 两条早就改了** — `item_attack.dm` 的 `attack_message_spectator` 是**局部变量赋插值串**，codemod 够不着；同一个 proc 里紧挨着的另两条都有 NOVA EDIT。症状很有辨识度：`You beat John Doe的胸部 with the stunprod!` —— 英文框架里嵌着一个中文部位名（`get_hit_area_message` 早就本地化了）。**「同一处消息的三个视角里只有一个是英文」是这类的判据**。手工接 LANG 时注意顺序陷阱：改写之后字面量就没了，`extract` 再也看不到它，英文源串必须自己按 `nova-i18n key` 校验过的哈希写进目录。
- **上游对同一组消息有「基类 + carbon 覆盖」两份，只改了基类那份 = 高频路径整类没翻** — `item_attack.dm` 的 attacker/victim 早有 NOVA EDIT，`carbon_defense.dm` 里 carbon **覆盖**的同一组六条（spectator/victim/attacker × 有无 `extra_wound_details`）却一条没接 —— 而人被打走的正是 carbon 那份，于是这一整类是实际最高频的漏翻。判据是漏翻串**逐字节匹配英文模板**：`You attack John Doe的胸部 with the stunprod!` 与 `You {0} {1}{2} with {3}{4}!` 严丝合缝，说明用的就是英文模板而不是「翻了一半」。查同类：拿基类里已 LANG 化的那条消息去 grep 它的**原始英文形态**，看有没有别的文件还留着裸串。
  连带收获：这六条接上 LANG 之后，`extra_wound_details` 这个局部变量第一次进入「局部变量一跳」规则的视野，它的字面量（`", threatening to sever it entirely"`）自动被抽了进来 —— **接一处 sink 会顺带解锁喂给它的那些局部变量**。
- **一整行里可以既有模板又有纯串；模板命中就提前返回 = 后面的英文永远轮不到 AC** — 职业描述的 antag opt-in 段是**三段后缀拼在一起**：第一段 `" Forces a minimum of {0} antag opt-in."` 是带占位符的模板、后两段 `" Targetable by contractors."` / `" Targetable by heretics."` 是纯串。`lang_localize_chain` 在模板命中后 `return`，后两段永远落不了地 —— 症状是「第一段中文、后两段英文」。改成模板命中后**继续走 AC**（已翻好的中文不含拉丁字母，AC 不会再碰它）。注意这与「整串精确必须排在模板之前」不冲突：那条说的是**谁先跑**，这条说的是**跑完要不要接着跑**。
  **这条我错判了两轮，教训在测试串上**：⑥b 我手写的是 `"Forces a minimum of …"`，而目录模板的第一个字面段带**前导空格**（`" Forces a minimum of "`）。少一个空格锚就匹配不上 → 模板不命中 → 走 AC → 全都翻掉 → **测试通过**，于是我判定「模板早返回」这个假设不成立，转头去查时序、查第二调用点。**照抄真实形态，别手写等价物**——这条 AGENTS 里为 `i18n_suffixed` 写过一次（DM 续行的制表符），同一个坑换了个形态又踩一次。定位手法：漏翻日志的邻近行（那几条是 species 参数与 CKEY 白名单）指出这是**偏好资源构建**期，据此才找到 `lang_localize_job_description` 这个调用点。
- **「组件自定义 prop → 组件内部渲染成可翻 prop」是 TGUI 抽取的一条稳定盲区** — `<ReplaceObjectivesButton button_title="Evolve New Directives">` 里 `button_title` 不在 `translatable_props`（不该在，全局加它误伤太大），但组件内部是 `content={button_title}`、`content` 在表里 —— **渲染期照常本地化，缺的只有抽取**。同形的还有 `<SingleLoadout name=… blurb=…>`（内部 `<Section title={name}>`）与 `<BeakerSectionDisplay title_label=…>`。按「组件 + prop」定点登记（`COMPONENT_PROP_LABELS`），不动全局清单。**判据**：漏翻报告里那串在源码里是个 JSX 属性值，而属性名不在 policy 里 —— 跟进去看组件内部把它渲染到哪个 prop 上。
- **解构里的默认值不在任何显示位置上，抽取器看不见** — `const { button_tooltip = '…' } = props;` 这类默认值既不是 JSX 属性值也不是 children。与其为它单开一条抽取规则，不如把默认值**挪进 JSX**（`tooltip={button_tooltip ?? '…'}`）——可翻 prop 上的 `??`/`||` 字面量操作数 `addDisplayExpr` 本来就收。改一行源码比加一条规则划算，也不扩大准入面。
- **「局部变量一跳」在 TS 侧比 DM 侧更准，因为准入面可以直接看渲染位置** — `const modeText = cond ? 'RegEx Mode' : 'Standard Mode'` 之后 `{modeText}` 渲染：**运行期本来就翻得动**（JSX children 走 auto-localize），缺的只有抽取。准入面用「该标识符是否出现在**显示位置**」界定（JSX 表达式 children，或可翻 prop 的值），而不是靠形态猜——DM 那条只能拿「是不是 LANG 实参」当近似。用作 `act()` 载荷的局部变量不在显示位置上、天然排除；两者兼有时（`content={x}` + `act(x)`）也安全，auto-localize 只改渲染出来的文本、`act` 送的是变量本身。
- **`CodexGigas` 那 29 条不是一类，我一开始判粗了** — `PREFIXES`（`Dark`/`Hellish`）、`TITLES`（`Lord`/`Prelate`）、`SUFFIXES`（`the Red`/`the Soulless`）都是**真词**、是玩家要读着挑选的按钮文字，该翻；只有 `NAMES`（`hal`/`ve`/`odr`）是**音节**，没有词义也没有对应的中文音节，不收。关键证据是 `onClick={() => act(title)}` ——**回传的是原变量而不是渲染出来的文本**，所以 DM 侧拼出的恶魔名始终是英文，按钮翻成中文不会造出中英混排的名字。「整个界面不该翻」这种判断要落到**每张表**上再看一遍。
- **`ColorMatrixEditor` 的 `rr:`/`gr:` 确实不该翻** — 它们是 `${PREFIXES[row]}${PREFIXES[col]}:`、PREFIXES 为 `['r','g','b','a','c']`，即 RGBA 颜色矩阵的行列坐标，中文同样写 r/g/b/a；而且是运行期由单字母拼出来的，本来也成不了目录键。
- **显示位置上的**函数调用**，它的 return 字面量同样是文案** — `{check_attempts(attempts_left)}` 里那个函数 `return cond ? 'on next failed access attempt.' : \`after ${n} failed access attempts.\`` 既不在 JSX 里、也不是变量赋值，walker 与「局部变量一跳」都够不着。把它并进同一条规则：JSX 显示位置上如果是 CallExpression，记下**被调用者的名字**，再收该函数的 return 字面量（箭头函数的简写体没有 return 语句，要单独认）。这是 DM 侧 `is_display_descriptor_proc` 的 TS 版，但准入面更准 —— 那边靠 proc 名白名单，这边直接看「它的返回值有没有被渲染」。
- **组件 prop 的值常包在 `&&` / 三元里**（`title_label={recording && 'Virtual beaker'}`），按组件登记时要走 `propTemplateBranches` 逐支收，只认整段字面量会漏。**同一个组件可能有两个名字**（`BeakerDisplay` 与包了一层 Section 的 `BeakerSectionDisplay`），登记时都要写上——只写一个的话报告里那条会一直挂着，而你以为已经登记过了。
- **`propTemplate` 的运行期闸门会挡掉一些本该翻的，那是对的** — `tooltip={blocked ? 'Fill in the following lines: ' + getErrorText(…) : …}` 的模板 `Fill in the following lines: {0}` 已在目录，但运行期捕获值是 `target, name, job, message text`（多词且小写开头）→ 被「捕获值要像值不像散文」那条挡下，整条保持英文。**别为这种个案放宽闸门**：那条闸门挡住的是「17 条 `Select a policy to view…` 被泛化骨架吃掉」那一类，代价大得多。
- **「整串精确赢过泛化模板」这条在「整行未切块」作用域上还漏着一处** — `lang_fallback_apply_html` 为了让「字面段里带标签」的模板（含 `<a href>` 的投票行）匹配，会先在**整行**上跑一遍模板引擎；那条 pass 排在整块精确查表之前，于是 `You feel like {0}.` 把 `<span class='notice'>You feel like you could be safe on your own.</span>` 吃成「你感觉像you could be safe on your own。」——**整句译文一直躺在目录里，只是轮不到**。同类还有 `You no longer feel {0}.`、`You start {0}{1}.`：一轮 harvest 里 fallback 桶 71 条有约 20 条是这一个缺陷，外加它配对的 arg/capture 记录。这与 `lang_localize_chain` 里那条（i18n_real_catalog ①c）同源，当年只修了**块**作用域。
  · **闸门的判据必须是「整行只有一个文本块」，不能是「存在某个块精确命中」**。目录里躺着 `You can`、`" and "` 这类拼句碎片（字面 AC 那几起事故的主角），按「存在即否决」写会把**正经的跨块模板**一并否掉：`You can <b>examine closely</b> to learn a little more about {0}.` 的框架分落三个 chunk、只有整行 pass 够得着，而 `You can` 恰好命中 → 模板被否 → 玩家看到「你可以 凑近细看 to learn a little more about…」。**这一版我先写错了一次，`i18n_html_tag_keys` ③ 当场抓住**。道理：单块时切块器一定翻得动整行、模板不可能做得更好；多块时模板跨块工作，该让它赢。
  · 症状与「AC 碎片类」的区别：这里是**中文句式裹着成段英文**，而不是英文句里嵌一个中文词。
- **落在 HTML 标签内部的 LANG 实参收的是标识符，不能翻** — 幻觉心灵感应那条模板是 `<span class='{0}'>…</span><span class='{1}'> {2}</span>`，`{0}`/`{1}` 是 span 的 **CSS 类名**（`boldnotice`/`alien`），`{2}` 才是文案。送进 `lang_localize_arg` 轻则刷漏翻日志，重则某天目录里恰好有同形条目 → 中文被写进 class 属性、聊天配色当场全丢。调用点无从分辨（同一条模板里三个占位符待遇不同），但**形态**分得清：只有 `<` 与 `>` 之间的占位符是属性值。`lang_interpolate` 现在跨字面段累积 in_tag 状态（`lang_tag_state_after`），无标签的模板预判一次、零额外开销。测试要拿一个**确实翻得动**的值当类名（`drowning`），换个查不到的词就什么都测不出来。
- **漏翻探针的 `origin` 决定这份数据能不能用** — `arg` 桶从前只记值，而实参多半来自 `pick()` 词池：拿 `combative` 去全仓 grep**根本搜不到**（它在 `strings/names/adjectives.txt` 里）。把 `lang_resolve` 的 key 一路传到 `lang_localize_arg`（`lang_interpolate(template, args, key)` → `lang_log_miss_value(arg, "arg", origin)`）之后，211 条实参一次归到 34 个 key 上：**76 条来自两个 junk 邮件 key 的同一个形容词槽**、8 条是披萨盒标签、3 条是攻击动词——每一组就是一条修法。`miss-scan.mjs` 按 `<ns>.<hash>` 形态聚合。判据：任何「按值聚合看不出调用点」的桶，都该问一句「这一层能不能把 key 带上」。
- **`attack_verb_continuous`/`attack_verb_simple` 在 SINK_VARS 里，却一条都没进目录** — 看着像早就覆盖了，实际是 **list 初值**：SINK_VARS 那条路对 list 走 `build_template` 而它对 list 返回 None，激进 pass 又要求句末标点，小写单词动词（`strikes`/`roasts`/`scorches`）两道都过不了。于是「表里有名字、目录里没条目」，战斗消息里动词整片英文。修法与 `speak_emote` 同路（`emit_list_strings` 逐元素抽），落地靠 `lang_localize_arg` 的**整串精确**反查（无多词门槛）。**这一类要先量化再放行**：全仓 715 条，其中 37 条与 DM 标识符碰撞（`ban`/`scan`/`hack`/`heal`/`stun`/`update`…），进主目录就是往全局反查表里塞这些词对。挡法是 `ATTACK_VERB_IDENT_BLOCKLIST` + 「改完跑 `nova-i18n lint`，碰撞告警不许比基线多」这条可证伪的维护约定。
  · 被挡下的那批**没有便宜的去处，只能留英文**。我按 AGENTS 原来那句「同形异义的显示词进 `_state_words.json`」把 9 个词写了进去 —— **那句话是错的**：`_state_words.json` 并没有作用域隔离。`build_i18n_cache` 把 locale 目录下**全部** .json 合并进 `i18n_cache`，`lang_build_reverse` 又直接从 `i18n_cache` 建表，所以 `_state_words` 的每一条同样进**全局反查表**（它"域内"的地方只是 `lang_localize_arg` 会先查它一次）。写进去当场把碰撞告警从 51 顶到 63，正是黑名单要挡的东西。**判据**：任何放进 `strings/i18n/<locale>/` 的 .json 都在全局反查面上，与文件名无关（`tgui.json` 那条早就说过，这里是同一个坑换了个文件）。真要做域内表，得先让 `build_i18n_cache` 或 `lang_build_reverse` 认这个例外。
  · 顺带一个有用的测量：扫「这 37 个词有没有以**字面量**形式出现在 LANG 实参位置」，全仓只有 4 处，且全是显示动词（`lethal ? "stun" : "kill"`、`grinding ? "grind" : "juice"`、`vampire_charging_enabled ? "charge" : "cook"`）—— 说明这批词在实参位置上几乎不当标识符用，将来若真做出域内表，它们是安全的候选。
- **词池（`world.file2list` 读的 .txt）按 locale 换整张表，别把词塞进目录** — `GLOB.adjectives` 是 375 个极常见英文单词（`hot`/`in`/`real`/`kind`/`solid`…），进全局反查表就是凭空扩大整个 DM 侧的误翻面。`lang_word_pool(filepath, fallback)` 按 `<名>.<locale>.txt` 同目录选表（与 `lang_speech_replacements` 同一条路子的 .txt 版），**必须惰性**——`GLOBAL_LIST_INIT` 跑在 locale 读入之前。两个细节：`world.file2list` 不经 `GLOB.string_cache`（所以不受「只按文件名索引」那条约束）；末尾换行会读成空元素，而 **`list -= ""` 每次只摘一个**（`list -= null` 那条同源），要循环着删。
  · 换表的**颗粒度是调用点而不是 GLOB**：`GLOB.ing_verbs` 同时被梦境和管理员匿名名字生成器用，后者必须保持英文。
  · 梦境（`dreaming.dm`）是 `dream_strings`+`adjectives`+`adverbs`+`ing_verbs`+`verbs` 五张表拼出来的，**要换就得五张一起换**——只译形容词会产出「a 迷人的 admiral」，比全英文更难看。
- **MT 的形态判据要给 scope 证据让路** — `attack_verb_*` 是纯显示（战斗消息里 `[user] [verb] [target]`，不参与任何比较），可 `stripNoise` 把 kebab-case 当标识符剥掉（`shin-dig`/`power-punch`/`scruggs-style`，其实是班卓琴的攻击动词）、`hasTranslatableEnglish` 把单个全大写词当缩写（`HONK`/`MEAT`/`SQUEEZE`），两类都永远进不了待译队列，症状看着像「个别词漏译」。判据用 `scopes.json`：scope 以 `#attack_verb_continuous|simple` 结尾就是文案。与 `isAtomDisplayName` 同一条路子——**scope 是比字符串形态更强的证据**。
- **`nova-i18n lint` 规则 D 必须排除 `unit_tests/`** — i18n 的落地层测试**必须照抄真实渲染形态**（AGENTS 里「别手写等价物」那条），于是每写一条回归断言都会被规则 D 报成「新增裸英文」而 CI 变红。抽取侧早有同样的排除（`in_unit_tests`），lint 侧漏了。注意 DM 的 file list 用反斜杠，判断前要先归一。
- **`pick()` 是独立 AST 节点，不是 `Term::Call` —— 全仓 pick 词池整类没抽（466 条）** — DM 支持权重语法 `pick(50;"a", 50;"b")`，所以 SpacemanDMM 给了它 `Term::Pick(Box<[(Option<Expression>, Expression)]>)`；`recurse_term` 只列了 Call/List/InterpString… 没有这一支，于是 `pick(...)` 里的字面量**从来没有被 visit_expr 访问过**。而 `pick()` 正是 DM 写随机 flavor 消息最常见的形式（试剂效果、随机吐槽、随机描述）。
  · **判据是「同文件内的反差」**：`alcohol_reagents.dm` 里 147 条普通句子有 146 条在目录，8 条 `pick(...)` 里的一条都不在。普通句子是靠 **sink 路径的 `build_template`** 抽到的（与激进 pass 无关），所以缺口只在 pick 上显形 —— 单看「目录里有很多这个文件的句子」会误判成已覆盖。
  · **定位手法值得记**：连着两次读代码都判错（先怀疑 InterpString 没下探——其实有；再怀疑 `pure_procs` 按 proc 名连坐——探针证否）。最后是在 `visit_expr` 入口打一条**无条件**探针（不受 `!suppress` 门控），发现该字面量在整个抽取过程里**一次都没被访问**，才把范围缩到「traversal 根本没走到」。**探针要打在门的外面**：打在门里面（`!suppress` 之内）时不触发，既可能是"没走到"也可能是"被抑制了"，两种情形分不开。
- **`"显示名" = /类型/路径` 的 assoc 表，键就是文案** — 午餐盒菜单、怪癖赠品选项这类 `GLOBAL_LIST_INIT(x, list("Space Beer (Canned)" = /obj/…, …))`，`emit_list_strings` 对 AssignOp 取的是 **rhs**（类型路径不是字符串 → 跳过），键整类丢了。**值是类型路径**就证明键不是程序查表用的数据键，而是一张「标签 → 类型」的展示表（`examine_tags` 那条「assoc 键即文案」的另一种形态）。键同时被当 act 回传标识符用（`GLOB.x[value]`）也不要紧 —— 负载不动数据、译文随 overlay 下发，前端只翻显示。仍走多词闸门。全仓 251 条，零碰撞。
  · `GLOBAL_LIST_INIT` 展开成 `/datum/controller/global_vars/proc/InitGlobalX() { X = list(…) }`，所以它在 **proc 体**里，走的是 visit_expr 那条路，不是类型变量那条。
- **`strings/` 下的 flavor 数据文件要逐个登记（`flavor.rs FLAVOR_FILES`），漏一个就是整类没译** — `hallucination.json` 268 条里 246 条不在目录，玩家在幻觉里"听到"的全是英文。登记时要**按 section 挑**：`weird` 是乱码（`#@§*&£`）翻不得，`add_name` 只有 `%TARGETNAME%` 占位。
  · `people`/`accusations`/`contraband`/`threat`/`location` 是**碎片**——`fake_chat.dm` 用裸字面量框架按英文语序拼成整句（`"[people] is [accusations]!"`）。**碎片与框架必须同一次做完**：只译碎片会产出「舰长 is 叛徒!」，比全英文更糟；只接框架则句子里嵌着英文名词。框架接 LANG 之后语序在**译文侧**调（`{0} in {1}` → `{1}有{0}`，"Bomb in cargo" → 「货舱有炸弹」）。
  · 这批框架**短到进不了模板逆匹配引擎**（`I18N_TPL_MIN_ANCHOR` 要求锚 ≥10 字节且含空格，而 `" is "`/`" in "` 只有 4），所以把 `{0} is {1}!` 这种泛化骨架放进目录**不会**引发「中文脚手架裹英文」那类劫持——它们只走 LANG 正向取键。加这类短模板前先对一遍锚长。
  · `[pick("C","Ai, c","Someone c","Rec")]all the shuttle!` 是**拼字母**的写法（"C"+"all"、"Rec"+"all"），碎片没有词义、翻不了 —— 只能改写成四条完整句子各自入目录。见到 `pick` 的元素拼起来才成词的，一律按整句重写。
  · 碎片池全是短名词，实测有 6 条（`cargo`/`Air`/`cult`/`Energy`/`hulk`/`Infinity`）与 DM 标识符碰撞 → `FLAVOR_IDENT_BLOCKLIST` 挡掉。取舍很清楚：幻觉里少译一个词 vs 弄坏 `switch`/下标查表。
- **`english_list()` 出现在 LANG 实参里 = 列表项全英文 + 英文 "and" 混进中文句子**（全仓 43 处）— 整串是运行期 Join 出来的、永远不是目录键，而每一项都还是英文。换成 `lang_english_list()` 逐项过显示边界、用顿号连接。**`and_text` 的语义必须留住**：全仓实际用到的连接词里 " or " 有 12 处（异常锁的备选核心、手术备选工具），一律替成顿号会把「或」的意思弄丢；按词表映射（and/,→顿号，or→或），认不出的退回顿号——宁可少一个词，也不要把英文 "or" 夹在中文列表里。
- **`_state_words.json` 并没有作用域隔离，别照 AGENTS 旧说法往里塞词**（已更正，见攻击动词那条）— 判据：`strings/i18n/<locale>/` **目录内**的 .json 全部进 `build_i18n_cache` → 进全局反查表；真正的域内表是 `strings/i18n/` **顶层**那几个（`slime_colours.json`/`wire_colours.json`/`statpanel_tabs.json`），它们不在任何 locale 目录里，`lang_scoped_table` 单独读。要新建域内表就放顶层。
- **词表 MT 的失败模式是「整张表丢掉」，必须能降级** — 929 行的 `ing_verbs` 只要有一批反复对不齐行数，整个文件就没产出（而这类表没有 key 可以校验，错位一行后面全挂错词）。`word-pool-mt.ts` 现在分三层兜：超时/网络抖动重试 3 次 → 行数对不齐先整批重试 2 次 → 仍不齐就**二分继续**，最后单行还不齐就保留英文并告警。少译一个词远好过整表错位，更远好过整表丢失。
- **译文里的 `\the`/`\a`/`\A`/`\an` 是**无害**的，别去批量清理** — 扫出 933 条中文译文含这些宏时我差点动手改，但 `lang_process_text_escapes` 的 `i18n_article_macro_regex` 早就把它们连同后面那个空格一起剥掉了（`\s`/`\es` 另一条规则单独处理，因为紧贴单词）。**先确认落地层有没有处理，再决定要不要改数据** —— 这类"看着像 bug 的数据"改下去就是几百条无谓的 diff。
- **`strings/` flavor 文件要一个个登记，`exodrone.json` 又是整份漏的** — 探索无人机的事件文本、星球描述、外星生物名（23 条）经 TGUI 直接展示。挑 section 的判据同 hallucination：`probe_names`（Cassini/Galileo）是探测器专名不收；`trading_station_names` 是商号专名，而且被 `name = "\"[x]\" trading station"` 拼进英文框架 —— 只译商号会得到「"中文" trading station」这种半截货，**框架不动就别动碎片**。
- **`traumas.json` 的 `@pick(x)` 子池**：框架（`brain_damage`）早就登记且译好了（`@pick(george) @pick(mellens) 在搞我，救命！！！`），子池没登记就是中文句子里嵌英文碎片。收 `create_nouns`/`create_verbs`/`mutations`/`bug`（都是「拼错的真词」：`abdoocters`=abductors、`eppilapse`=epilepsy）；**不收** `servers`（basil/sybil/terry 是 /tg/ 服务器专名）、`george`/`mellens`（人名），以及 `random_gibberish`/`y_replacements`/`semicolon` —— 后三者是**字母级**变换表，翻了当场把「拼错」这个效果弄坏。
- **`"显示名" = list("bottlepath" = /obj/…, "reagent" = /datum/…)`：类型路径表可以嵌一层** — 上一条「值是类型路径 ⇒ 键是标签」的规则只认光秃秃的 prefab，而酒瘾/怪癖赠品这类表的值是一张小 assoc（全是类型路径）。判据不变（值全是类型路径），内层的 `"bottlepath"`/`"reagent"` 是数据键但都是单 token，被多词闸门自己挡住。
- **「一边像标识符、另一边就是显示标签」这条对称判据不能用，收 686 条、当场报错** — 想一次收掉 `"ghost_dcyan" = "Dark cyan"`（值是标签）与 `"Cold adapted lungs" = "cold"`（键是标签）两种形状，写成「一边单 token 标识符 + 另一边多词文本 ⇒ 多词那边是标签」。加了「标签必须 Title Case」仍有 686 条，`nova-i18n lint` 报 **1 错误 + 20 条新碰撞**：化学反应参数（`Optimal Max Ph` = `optimal_max_ph`）、性能面板（`SendMaps: Cleanup` = `cleanup`）长得一模一样，而它们的「标签」正是程序拿去查表的键。**这类「靠形状分主次」的判据在 assoc 表上普遍不成立** —— 同一个形状既是菜单也是参数表。
  · 退回到**槽位名**判据：只认 `name`/`label`/`title` 这几个键（`list("name" = "Plastic Jungle Rocks", "path" = /obj/…)`，RCD/RTD 造物设备菜单）。310 条、零碰撞，而且恰好是那 686 条里真正玩家可见的主体。代价是 ghost 颜色名与适应肺那两张表仍是英文 —— 明确的取舍，不是遗漏。
- **`nova-i18n extract` 会往 zh 目录写东西（译文迁移），所以「只 `git checkout` en 再重抽」会留下一地孤儿键** — 迁移过的 zh 条目留着、en 侧被 checkout 抹掉 → `[catalog] zh-Hans 有 key 但英文目录已无`，一次调规则的过程里攒了 69 条，把告警从 54 顶到 125。**回滚抽取结果时 en 与 zh 要一起 checkout**；已经攒下的用「zh 有 en 无」扫一遍摘掉（`_state_words`/`_fallback` 是手维护的例外，两边本来就不对称，别动）。
- **目录只增不减这条对「刚抽错的键」同样成立** — 加完黑名单重跑 extract，脏键还在（merge-only），lint 照旧报。**改黑名单之后必须手工把那条 key 从 en/zh 里摘掉**，光改代码不动数据等于没改（`Other Stuff` 这条就是这么绕了一圈）。
- **钉死的「源码根」清单是悬空 key 扫描的盲区，`interface/` 就在盲区里** — `lint.rs` 的规则 C 走 `for dir in ["code", "modular_nova"]`，而 `interface/interface.dm` 同样 `#include` 进 .dme、同样被 rewrite 改写过。实测那里躺着 **8 个悬空 key**（wiki/rules/forum/github/config 几个 verb 的整句提示 + 表情面板），玩家点一下 verb 就看到 `client.b6e02a75` 这串乱码，而规则 C 一直报 0 —— **「门禁报 0」与「真的是 0」之间隔着一张它扫不到的文件清单**。这批 key 还是 v1 的 8 位形态：v2 换 key 那一趟同样只走了那两个根，改写与目录都没跟过去。修法是**从 .dme 的 `#include` 推导源码根**（按目录过近似即可，间接 include 不在扁平清单里，按文件收口会重新造盲区）。同类排查：任何 `["code", "modular_nova"]` 字面量都是下一个盲区。
- **生成 sidecar 也是 merge-only，而 GC 只覆盖目录文件** — `catalog.rs write_scopes` 与 `load_dir` 同样「只合并不裁剪」（理由正当：被 rewrite 改写过的 key 本次抽不出，scope 要从旧文件续命），但新的 catalog-audit 只看 `en/*.json`，于是 `scopes.json` 里攒了 **901 条死条目**，含换 key 方案时的中间态命名空间（`#atmos_scan().f540f85f` —— 那正是 `keys.rs` 注释里说的「凭空造出 400 个 `#xxx().json`」的产物）。功能上无害（MT 按目录 key 查 scope，死 key 查不到），但它是「目录只增不减」这条坑在治理层下的复现。修法：`write_scopes` 按 **live = 本次抽出的 ∪ 源码里仍被 LANG 引用的**（与 audit 的 `live_keys` 同一判据）裁剪，`live` 必须在 `load_dir` **之前**定格 —— 合并之后历史条目与本次抽取就再也分不开了。catalog-audit 现在也扫 sidecar，复发会当场报错。
- **`DM_LABEL_SOURCES` 的路径失效是静默的，且被「目录只增不减」二次掩盖** — 登记表里 6 条路径已不存在（上游把 `code/datums/sprite_accessories.dm` 变成了同名**目录**，Nova 侧几处 personality/loadout/sprite 目录整个搬走）。后果不是报错而是**整类标签退出抽取面**；而 tgui.json 从不裁剪，历史条目还在、界面照常显示中文，所以**从界面上完全看不出来**，只有在「统计哪些 key 还活着」时才现形。这类登记必须**路径不存在即硬报错**——静默跳过等于把「上游改名」变成一颗只在下次裁剪目录时才引爆的雷。
- **tgui.json 的 union merge 是承重的，绝不能照 DM 侧的 GC 依样画葫芦** — 直觉上「抽取不再产出的 key = 死键，该裁」，实测**不成立**：TGUI 运行期有多条 fallback 会拿**渲染出来的任意字符串**回目录里查（`localizeChildrenSegments` 的逐段查表、overlay、auto-localize），这些串没有任何静态抽取路径会产出。`Make Announcement`（`RequestsConsoleMainScreen.tsx`，`文本 + <Icon/>` 混排 children）与 `Send To All`（`NtosMessenger`）今天就是这么被翻译的 —— 它们不在 `tgui-scopes.json` 里，按「死键」裁掉就是整片界面回退英文。判据：**DM 侧的 key 是 LANG 按 key 正查（可枚举），TGUI 侧是按渲染文本反查（不可枚举）**，所以 owner=tgui 的文件被排除出 GC 是设计而不是遗漏。
- **收紧字面 AC 的准入闸门，两个「显然对」的规则实测都误杀** — 目录里确实躺着能从单词内部开火的死碎片（`", Col"→", 列"` 会把 `Smith, Colonel` 咬成 `Smith, 列onel`；反派介绍那批 `- Make yourself a`、`...at [` 同理，全仓 425 条）。但顺手收紧会出事：① 把「首/尾是虚词」的判据从 ≤3 词**扩到所有词数**，会连 `a pair of wirecutters`、`a fine cutting tool` 一起毙掉（首词是 `a`）；② 加「首/尾 token 必须含字母」，会毙掉 `Armoury no. 09`、`Interlink Cabin 3`、`Khonsu 19` 这类**以编号结尾的区域/物件名**。两条各自砍掉 1800+ 条正当条目。真正的碎片签名是**「整串以标点起头」**（`,` `-` `...` `[`），而且必须先剥掉文法宏（`\improper Bar` 不是碎片）。这条值得单独一趟做，配真目录测试，别当顺手活。
- **拼接链要按「运行期切块器怎么切」来拆，不是按 AST 怎么方便拆** — `to_chat(span_notice("A") + span_boldnotice("B"))` 里 `build_template` 把两端 span 标签当作「去标签后无字母」丢掉，两句折成**一个** key `"AB"`；而 `lang_fallback_apply_html` 按标签边界切块，切出来的 `A`、`B` 谁都不等于那个 key —— 译文躺在目录里却永远查不到（实测 tackle 组件那条）。`build_tag_chunk_templates` 按操作数拆：每个操作数要么是**标签包裹的显示单元**（首尾字面量是纯标签），要么是无字母的分隔串，否则整条交回 `build_template`（`"You have " + count + " items"` 运行期不会被切开，拆了只会产生查不到的半句）。
  · **分隔串判定必须先剥 DM 转义**：AST 里的 `"\n"` 是反斜杠加字母 `n` **两个字符**，直接看 `is_alphabetic` 会把它当正文，于是「两个单元之间夹换行」的形态拆不开。
  · 拆开后旧的折叠 key 若已有译文，**手工按语义切分搬到两个新 key 上**，别让它白丢。
  · 规模预期要放低：这个形状全仓只有 1 处调用点。「块是某条已译目录项的真子串」实测 35 条，其中只有 5 条是这类**边缘**（拼接/切块），另外 30 条是 LANG 实参/模板捕获值查到了句子**中段**——那是另一个问题，别混为一谈。
- **漏翻采集器的桶要跟着架构走，否则整份报告被噪音淹掉** — 重构后 `tgui.json` 不再进 DM 侧目录桶（`catalog-domains.json` 标 `domain: tgui`，加载器直接 `continue`），于是 P1 对每个负载值都找不到译文、条条刷 miss ——而**前端 bundle 里有译文**，玩家看到的是中文。实测一轮 419 行里 81 行是这个，全落进「已译未接通」，把真正的 13 条缺口埋了。`miss-scan.mjs` 加「前端已覆盖」桶（查 `tgui/packages/tgui/i18n/<locale>.json`），**只对 DM 侧来源生效**：`tgui-ui` 是前端自己报的 miss，它出现在 bundle 里反而说明有别的问题，不能降噪。
- **运行期加缀的实例名有前缀、后缀两个方向，别只做一个** — `lang_localize_type_affixed_name` 认「`initial(name)` 是前缀」（法则架 `"core module rack 'alpha'"`、贴标机 `"beaker (盐)"`），而材料造物是反的：`/obj/item/coin/gold` 的运行期名是 `"gold coin"`，`initial(name)` 在**后缀**位。基础类型名（`coin`→硬币）早就在目录里也翻好了，缺的只是这个方向的拆分。闸门用「前缀必须是已登记材料」（`lang_material`/`materials.json`），不设就等于放行任意运行期前缀。判据陷阱：先别急着判「整类漏抽」——拿基础名去 grep 目录，在且已译，就说明缺的是拼接形态而不是抽取。
- **收紧字面 AC 的闸门：判据是「续接标点打头」，且必须逐条量过再落地** — 三轮实测才收敛。只认 `,` `;` `:`，以及 `.` / `-` **后面跟空白或另一个句点**；必须排除的三类（各自都在真目录里成百上千条）：引号打头的区域专名（`'Time Of Valor 2' Medbay`，700+）、`.` 后跟数字的弹药口径（`.357 Phasic Speed Loader`）、`(`/`&`/`*` 打头的完整标签（`(WALL PIERCE)`、`&bull; …`）。最终精确剔除 153 条，逐条过目全是拼句碎片。**别用「首字符是标点」这种粗判据**（剔 406 条，误杀一片），更别用「首尾是虚词扩到所有词数」或「首尾 token 须含字母」（各砍 1800+ 条正当条目）。碎片仍留在目录里供精确查表，只是不再有子串开火的权力。
- **DM 字符串里的裸 `[` 会让 SpacemanDMM 从该行起整份文件解析错乱，而 DreamMaker 放行** — 写测试用例 `"...at ["` 时中招：`nova-i18n lint` 报一串 `illegal byte 0xe7`（其实是后面那些中文注释被误当代码），而 `build.sh dm` 是 0 errors 0 warnings。字面方括号一律写 `\[`。这是「DreamChecker 全绿≠能编译」的**反向**版本：两个解析器都要过，谁红都得修。
- **`nova-i18n lint --update-baseline` 会把碰撞基线一起吞** — 它同时重写 `bare-english-baseline.txt` 与 `identifier-baseline.txt`，一跑就把当前全部碰撞告警（实测 51 条）收进基线、告警数瞬间变 1。只想收裸英文那条时，跑完要 `git checkout -- tools/i18n/identifier-baseline.txt` 把另一半撤回去，否则「碰撞告警不许比基线多」这条维护约定当场作废。

- **`is_loose_sentence` 的句末标点闸门不认冒号 → 冒号收尾的标签行整类漏抽** — 同文件的 `is_examine_sentence` 一直认冒号，两条闸门对同一种形状给出相反判定。判据是**同一个 proc 内的反差**：`chemscan` 里 `Subject contains no reagents in their stomach.`（句号）在目录，`Subject contains the following reagents in their stomach:`（冒号）不在。全仓这类 90 条：回合结算经济汇总、犯罪/医疗记录、商人买卖清单、赏金、statpanel。
  · **但不能直接开冒号**：实测放开后涌进 511 条，绝大多数是管理员日志与调试断言（`{0} Invalid timer state: …`、`{0}:{1}:Assertion failed: {2}{3}`、watchlist 通报）——它们走 `message_admins`/`CRASH` 这些 `is_non_player_sink` 够不着的汇聚点，而「冒号 + 插值」正是日志行的典型形状。加一道「**不含占位符**」的闸门之后是 141 条、零噪音。
- **`span_tooltip()` 的文案住在 HTML 属性里，切块器按设计跳过属性** — 展开是 `<span data-component="Tooltip" data-content="Remove surgically." class="tooltip">`，而 `lang_fallback_apply_html` 只翻标签**之间**的文本。跳过属性是对的（`class`/`href` 翻了当场丢样式和跳转），但 `data-content` 按构造就是散文，别处再没有第二条路径碰得到它。症状判据：**同一行里锚文本中文、悬浮提示英文**（健康扫描仪的断肢/辐射/异物提示，译文一直躺在目录里）。修在落地层只认 `data-content` 这一个属性，不要逐个改调用点 —— `conditional_tooltip` 是 `#define`，且有一批提示是运行期拼的。
- **具名实参形态的 sink 需要在 `sink_arg_name` 里登记，否则按位取参会落到别的实参上** — `tgui_input_list(user, message = "Select an ability", title = "…", items = names)`：`sink_message_args` 有它（`[1,2]`），但 `sink_arg_name` 没有 → 第 1 位取到的是 `items`，提示语一个字都抽不到，`rewrite` 同样跳过。这类漏法**在源码里完全看不出异常**（调用点写得很规范），只能靠「玩家报某个弹窗标题是英文」反推。
- **「局部变量一跳」的两个盲区：`pick()` 右值与局部查表** — 两者症状相同：**模板译好了、实参全英文**。
  · `var/cause = pick("space being cold", …)` 后 `LANG(key, list(cause))`：`push_assigned_literals` 只认 String/Ternary，不认 `Term::Pick`（老鼠迁徙公告「由于space being cold，a pack of squeaking things已migrated到maintenance tunnels。」）。
  · `var/static/list/ops = list(WAND_OPEN = "Open Door", …)` 后 `LANG(key, list(ops[mode]))`：`collect_bare_idents` 要求 `follow.is_empty()`，`ops[mode]` 带一个 `Follow::Index` 就被跳过（安保门遥控器的「模式：Open Door」）。收**值**不收键 —— 键是 `#define` 出来的模式常量。
- **pick 词池的准入判据：同池兄弟 + 变量必须是 proc 内声明** — 词池成员多是无句末标点的小写短语，激进 pass 的整句闸门收不到；而 LANG 实参那条多词闸门会把 `pick("migrated", "swarmed", …)` 这种全单词动词池整个挡掉，切出最难看的结果（长的译了、短的没译）。
  · **判据用「同池里有没有多词显示文本」**：池成员按构造可以互换，一个位置既能填 `squeaking things` 又能填 `mice`，那它就是显示槽。全池皆单词的（`pick("attack", "disarm")`）不收。
  · **直接放开单词是错的**：实测涌进 134 条，里面有 `l_arm`/`r_leg`（部位键）、`forcewall`（法术 id）、`block`/`charge`/`evade`（战斗模式值）、`lizard`/`corgi`/`santa`（类型 id）、`star`/`spiral`（图标态）。
  · **变量必须是 `var/x = pick(...)` 这种 proc 内声明**，不能是对已有变量的赋值：`fluid_type = pick(AQUARIUM_FLUID_FRESHWATER, …)` 里 `fluid_type` 是 datum 类型变量，值会逃出本 proc、在**别的 proc** 里被 `in` 判定，本 proc 的「有没有被比较过」扫描根本看不见它。局部声明的生命周期完整可见，才敢按池收。
  · 配套的 `collect_compared_idents` 要同时认 `==`/`!=` **和 `in`** —— 成员判定同样证明这个值是查表键。
- **`#define` 出来的「键即文案」表走手工目录，别去猜形状** — `SURGERY_STATE_READABLE` 是 `list("Skin is open" = SURGERY_SKIN_OPEN, …)`，键是手术计算机上显示的状态行、值是位标志。它在每个调用点被展开成匿名 list 字面量，没有任何抽取规则认得（`examine_tags` 那条「assoc 键即文案」是**按 proc 语境**开的，不能泛化）。判据很干净：同一个 proc 里的裸字面量（`Ready for surgery`）在目录，`#define` 表里的 10 条一条都不在。这类闭集直接写进 `_surgery.json` 这种 manual/global_reverse 目录。
- **`git checkout -- strings/i18n` 会连 MT 结果一起丢** — 调抽取规则时要反复 checkout 重抽，而 MT 产出只在工作区。顺序上把 MT 放到**规则定稿之后**，中途别跑；跑了就别再整目录 checkout。
- **lint 基线要在当前树跑一遍控制组，别拿上次会话记下的数字** — 我按记忆里的「52 告警」判断新增了 6 条，实际当前树的控制组（`git stash push strings/i18n` 后跑 lint）是 54，真实新增只有 1 条。差的那 2 条来自别处的改动，白追了一轮。控制组两条命令就能跑出来。
- **tgui-core `Button` 的 children 类型是 `string`，塞不进 JSX 片段** — 「值兼标识符的显示名」在 Button 上不能用 `{cond ? <>Label</> : value}` 那套（tsc 当场报 `Type 'Element' is not assignable to type 'string & ReactNode'`），要走 `defineMessage` + `translator.text(...)`：返回的是 string，且是唯一**被抽取器认得**的显式消息形态。顺带一条：`translateCurrent('X')` 这种写法**不进抽取面**，`tgui.json` 里那些键是历史遗留（union merge 从不裁剪）才活着的 —— 新代码别再这么写。
- **拿真目录断言的那批测试，在伪 locale 门禁下会静默跳过** — 重构之后运行时只加载**当前 locale**，而 `i18n_html_tag_keys` / `i18n_real_catalog` 这些测试开头就有一句「zh-Hans 目录不存在就 return（精简签出时不误报）」。跑 `pseudo-test.sh`（locale=qps-ploc）时 zh-Hans 根本没加载 → 整个测试体一行没执行、照样报 PASS，耗时 0.0015s 就是指纹（对照 `i18n_display_boundary` 的 0.43s）。**新写的落地层断言必须用 `miss-harvest.sh`（真 zh-Hans）验一遍**，只跑伪 locale 等于没验。两个门禁互补这条一直写着，但没写明「哪些测试只在真 locale 那一边执行」。
  · 顺带一条：`data/unit_tests.json` 里 `status: 2` 是 **SKIPPED**（地图门控）不是失败，别按 `status != 0` 数失败数——我这么数过一次，把 9 条跳过的当成了 9 条失败。
- **同一句英文在两个 forward 命名空间里译得不一样 → 反查表整条略过（全仓 71 条）** — 重构把「按文件序挑一个」改成了「有歧义就不进反查表」，这个决定是对的（旧行为是按加载顺序随机取），但**代价没被量化过**：这些条目在所有反查路径上都够不着，正向 LANG 取键仍正常，所以只在聊天/examine/负载这些路径上显形。实测暴露的是 `You find yourself unable to speak!`（datum「说不出话了」/ mob「无法说话」）。
  · **修法是改数据不是改机制**：同一句英文被 MT 在两个命名空间各翻一遍、措辞小异的，统一成一条即可恢复覆盖（71 条里 68 条是这种）。
  · **别按「en 值分组看有几个 zh 值」算规模**，那样得出 273 条、里面大半是假的：`tgui.json` 不进 DM 反查表（它有自己的域），而 `global_reverse` 那批显式文件会**覆盖** forward 候选（`lang_build_runtime_domain` 里那句 `seen = list()` 就是干这个的）。按「forward 内部冲突 **且** 没有 global_reverse 文件给值」算，真实是 **71 条**。我先报了 273，差了 4 倍。
  · 由此还得到一条判据：`Engineering` 工程部/工程、`bolt` 螺栓/插销 这类看着最像问题的，恰恰**不是**问题——它们的另一半住在 `ui.json`/`manual.json`/`_wires.json` 里，那一层说了算。
  · **不是所有冲突都该统一**：`{0} is hit by \a {1}{2}!` 在 mob 与 obj 下英文一样，但 `{2}` 一个是被击中的器官、一个是「未造成伤害」的后缀从句，中文语序必须各自安排 —— 统一 = 破坏**正向**渲染（两个 key 各自渲染，值一改就错一个）。同形异义（`Lizard` 物种/动物）同理。这三条进了 `audit.rs` 的 `AMBIGUITY_ALLOWLIST`，每条写明理由。
  · 这一类由 MT 持续再生（同一句英文进两个命名空间 = 两个 key = 分两批送翻），所以清完必须配门禁，否则清一次长回来一次。已在 `nova-i18n catalog-audit` 里落地。
  · 顺带修掉一个真 bug：`custom_boxed_message("blue_box", …)` 的 **CSS 类名**在 obj 侧被译成了「蓝色_盒子」，样式当场失效。这类值要同时进 `keep-english`，否则下一轮 MT 看到 zh==en 又当成待译翻一遍。
- **`%VAR%` 宏在反查之前被替换掉 → 整条 key 再也对不上** — 抽取器照抄源码字面量，目录键里留着 `%MAXHEALTHRATIO%`；而 `New()` 里 `description = replacetext(description, "%MAXHEALTHRATIO%", …)` 一跑，运行期的串就不等于任何键了。**译文一直躺在目录里、界面恒为英文**，且在目录侧完全看不出异常。修法与 `announcement_system.dm` 的 %PERSON/%RANK 同源：**先反查整条模板、再替换**（译文同样保留占位符）。全仓目录里含 `%VAR%` 的条目 206 条，同形的就地替换调用点 8 处（dog_fashion、mood_events、cargo bounties、brimdust_sac…），本轮只修了被玩家报到的那条。
  · 连带：基础句反查之后再 `desc += " English suffix."` 就是「中文句 + 英文尾巴」，后缀也要各自反查。
- **`/datum/wires/proper_name` 不在 SINK_VARS** — 线路面板标题与 AI 的线路目录都显示它。45 条里 24 条**恰好**因为同名机器在别处入过目录而能落地，另外 21 条整齐地留在英文 —— 「同一份列表里一半中文一半英文」这个反差就是判据，而不是「整类没翻」。
- **`CATALOG_VALUE_BLOCKLIST`：按值挡，不要为一条值让整条规则退让** — `proper_name` 收进来时 `APC` 触发了 lint 的**高置信**碰撞（它同时是 polycircuit 的 switch 键）。那条规则其余 44 条都是正常显示名，退让整条代价太大。按值挡之后要记得：**目录只增不减，改完黑名单必须手工把那条 key 从 en/zh 里摘掉**（这一条 AGENTS 里写过，我隔了两小时又踩一次）。摘的时候只摘该规则写进去的那条 —— `tgui.json` 里同名的 `APC` 是 TGUI extractor 自己抽的界面标签，与它无关，误删会丢掉一条正常译文。
- **「同一份列表里按名字词数分成两半」= 运行期拼接 + 字面 AC 的多词门槛** — 基因扫描仪的 `"[mutation.name] ([alias])"`：多词突变名（舌钉、织网生产、妥瑞氏综合征）是中文，单词的（Spastic、Monkified、Cryokinesis、Telepathy）全是英文。名字本身早在目录里、也译好了，缺的只是**在拼接处逐个反查**。这个「按词数分半」的视觉特征比任何静态扫描都好认，见到就直接去找拼接点。
- **同形异义要用 `scopes.json` 的类型路径判，别按字符串形态猜** — `X chest` 在这个仓库里既是身体部位（`/datum/augment_item/limb/chest/...`、`/obj/item/bodypart/chest`）又是战利品箱（`/obj/structure/closet/crate/necropolis/...`）。按「译文里有没有『胸』」筛出 40 多条疑似，逐条看才发现绝大多数是**对的**；换成按类型路径筛，5 条错译一条不差地落出来（死灵城/龙/巨像/天使巨像/炽焰龙宝箱被译成了「胸部」）。scope 是比字符串形态强得多的证据 —— 这条与 MT 那边的 `isAtomDisplayName` 判据同源。
- **「类型路径末段当显示名」的兜底，只能进域内表，不能进主目录** — `/datum/chemical_reaction` **没有 `name` 变量**；反应查询界面对「没有产物试剂」的反应，显示名是运行期 `splittext("[reaction.type]", "/")` 取末段现切的（同条负载里的 `id` 才是标识符）。源码里没有任何字面量，抽取器按定义够不着 —— 玩家看到的就是 `omegasoapification`。
  · **先试了「按同样规则算出来塞进主目录」，不行**：全仓 114 条里混着 `heat` / `holy` / `life` / `soup` / `foam` / `spider` 这类通用单词（末段按定义就是标识符形态），`nova-i18n lint` 的碰撞告警当场从 54 顶到 61。加一道「必须自己声明 `required_reagents`」的闸门排掉抽象父类型之后仍是 60 —— **这条路没有安全的值侧闸门可设**。
  · 正解是 `strings/i18n/reaction_names.json` 这样的**顶层域内表**（`scoped:reaction_names`）：不被 `build_i18n_cache` 合并进反查表，只由 `lang_reaction_name()` 在那一个显示点查。骨架由 `node tools/i18n/reaction-names.mjs` 生成（幂等、只合并不裁剪），译文人工填。
  · 判据可以推广：**显示名来自类型路径/文件名/枚举键，而不是某个字面量** —— 这类一律走域内表。`i18n_real_catalog` ④ 正反两面守着（域内表要翻得动、`holy` 不许进全局反查表）。
- **拼接链能否按块拆，判据是「运行期会不会被切开」** — `lang_fallback_apply_html` 只在**标签边界**切块，所以标签包裹的操作数必然与左右邻居分属不同块。原先要求「所有操作数都得是标签包裹的」，把 `"Its all wired up[…]." + "\n" + span_info("You can use <b>wirecutters</b>…")` 这种「裸插值串 + 标签单元」挡在外面，折成一条谁也查不到的废键。放宽成：**每一对相邻的非分隔操作数里至少有一个是标签包裹的**。反面同样要守 —— 两个都不带标签的相邻操作数在运行期是连着的一整块（`"You have " + count + " items"`），拆开只会产出半句，必须整条交回 `build_template`。
- **整句进了目录，槽里的字面量也必须进** — 模板逆匹配捕获到的就是槽里那个值，捕获值再走 `lang_localize_arg`；查不到就原样留在中文句子里。`"Its all wired up[cell ? " and ready for usage" : ""].\n"` 即此：模板译好了，玩家看到「它已经全部接好了线 and ready for usage。」。**「这句整体会进目录」本身就是准入证据**，所以 examine 累加器与显式显示型具名实参（`poll_question`/`lmb_text`/`end_string`…）都该收槽里的字面量，闸门沿用 LANG 实参那条（多词）。
- **element / component 的构造实参要按类型逐个登记，不能按「长字面量」一刀切** — `AddElement(/datum/element/noticable_organ, "%PRONOUN_Their eyes move with machine precision…", BODY_ZONE_PRECISE_EYES)`：既不是 sink 实参也不是类型变量，整类漏抽。但**位置实参里标识符浓度很高**（`chav_replacement.json`、`snow_monkey_alive`、`bayonet_thin`、`mobs_killed_mining`），实测全仓 64 条未抽里一半是标识符 —— 按 element 类型登记（`element_display_args`），与 TGUI 侧 `COMPONENT_PROP_LABELS` 同一条路子。
  · **AST 里没有 "AddElement"**：`#define AddElement(arguments...) _AddElement(list(##arguments))`，预处理器展开在前，实参还被裹进一层 `list(...)`。按源码里写的名字匹配永远 0 命中 —— 与 LANG/LANGU 那条坑同源。第一版就是这么写的，白跑一轮。
- **`%VAR` 占位符必须在**反查之后**才替换，而且要有门禁挡 MT** — 这是第三次撞上同一个形状了（公告 `%PERSON`/`%RANK`、奇异试剂 `%MAXHEALTHRATIO%`、植入器官 `%PRONOUN_Their`）：目录键里留着占位符，运行期一 `replacetext` 就再也不等于任何键，**译文躺在目录里、界面恒为英文，且在目录侧完全看不出异常**。
  · MT 看不懂这类占位符：实测 25 条 `%PRONOUN_Their eyes …` 被翻成 `%PRONOUN_眼睛…`，代词那半截被吃掉，`REPLACE_PRONOUNS` 的正则再也匹配不上，**玩家直接看到 `%PRONOUN_` 这串东西** —— 比不翻难看得多。`catalog-audit` 现在扫「译文里出现、原文里没有」的占位符。
  · **允许少、不允许多**：中文常整条省掉人称代词，而且 `GET_TARGET_PRONOUN` 返回的是英文词（his/her/their），保留占位符反而会在中文句子里插进一个英文单词。
  · tokenizer 的三条边界都是实测逼出来的：`%SOMETHINGs`（英文复数贴在后面，首段遇小写就停）、`10% Auxetics`（首段必须整段大写，否则百分号写法被当成坏占位符）、`%PRONOUN_Their`（`_单词` 段可以首字母大写）。
- **「局部变量一跳」要能走多跳，种子还要包含 examine 累加器** — `magnitude`（switch 里赋的形容词）→ `label_line +=`（攒整行）→ `readout +=`（examine 累加器）这种**两跳**链上，一跳规则只够到 `label_line`，末端的 `"greatly increased"` 差一层，玩家看到「它对以下目标特别有效 beast and mining enemies, dealing greatly increased damage per hit.」。两处改动配合：把 `. +=` / examine 累加器右值里的裸标识符也当**种子**（它和 LANG 实参、sink 显示槽同为显示位置），再对「赋给已收集变量的值的插值槽」做不动点迭代。准入面没有放宽（种子与多词闸门都没动），放宽的只是「离显示位置几跳」。实测新增 33 条，全是显示散文（bane 伤害倍率形容词、伤口感染分级标签、灵能异象结尾、灵虚种充能建议），零标识符、lint 不变。
- **「纯占位符框架不是文案」这条判断把两件事混在了一起（21 处第三人称消息）** — `visible_message(span_danger("[施动者] [动词][复数s] [受动者][力度后缀]"))` 这类框架去掉占位符后一个字母都不剩，于是 `build_template` 按「不含字母 = 不是可翻文本」返回 None，整条调用不被改写。**前半句成立、后半句不成立**：
  · 实参仍然要过 `lang_localize_arg`（那里没有多词门槛），而**只有这条调用变成 LANG，实参才走得到它** —— 不然动词只剩字面 AC，而 AC 卡多词，单词动词（`punches`/`chucks`）永远是英文。
  · 中文往往要**丢掉**某个槽（英文复数 `\s`、`[verb][suffix]` 的后缀）或调换语序，这只有 zh 模板做得到（AGENTS 里「让 zh 模板不引用该实参」那条本来就依赖这一点）。投掷消息的 zh 模板就写成 `{0}{1}{3}{4}`，`{2}` 那个复数 s 直接不引用。
  · 闸门定在 **≥3 个占位符**：既是实测那一类的形状，也把 `"[msg]"`、`"[a] [b]"` 这种纯转发包装挡在外面（没有语序可调，LANG 化只是噪音）。激进 pass 仍走原来的 `build_template`。
  · **同一道闸门在三处重复出现，少改一处就整条链断掉**：`template::build_template`、`extract::emit`、`rewrite::node_template`。我先改了第一处，探针显示 25 处命中却一条没进目录 —— 第二处又否了一次；改完第二处 rewrite 仍然 0 处 —— 第三处还在。**排查手法**：探针打在**每一处闸门的外面**，看它在哪一层消失。
- **`for_each_statement_expr` 不覆盖 `Statement::Var` 的初始化式** — `var/t = cond ? LANG(key, list(magnitude)) : ""` 里的 `magnitude` 收不进「局部变量一跳」的种子，于是它那几个 switch 分支的形容词一条都不进目录，又是「模板中文、实参英文」。判据：`collect_local_assign_groups` 之所以要**单独**处理 `Statement::Var`，就是因为这个；任何新写的语句遍历都要照抄这一条。症状会以 `catalog-audit` 报 stale 的形式暴露（那批词原先靠别的路径进目录，改了调用形态之后就没人抽它了）。
- **`SCREENTIP_CONTEXT_*` 写在 list 字面量里的那批整类没抽（38 条）** — `context[SCREENTIP_CONTEXT_LMB] = "…"` 的**下标**写法早就抽到了，而 `list(/mob/living/silicon = list(SCREENTIP_CONTEXT_CTRL_LMB = "Toggle power", …))` 这种**列表字面量**写法没有。判据只能认**展开后的值**（`"Ctrl-LMB"` / `"Alt-LMB"` …）—— 那些是 `#define`，AST 里根本没有定义名，与 `BB_EMOTE_*` 黑板键同一条坑。APC 给 AI 看的那几条即此。
- **手工 LANG 化时 rule C 会拦 `name = LANG(...)`，那是对的，别一律加白名单** — 两处踩到：法医报告的纸张名（`"FR-[编号] 'Forensic Record'"`）是**新生成实例的合成身份名**、不参与比较，与 `mail.dm` 同形，进白名单；而 MOD 模块动作的 `name = "Activate [模块名]"` **撤回**了 —— `action.dm` 里有 `action.name == name` 的比较，且 `/datum/action` 没有显示边界可走。**判据是「这个 name 会不会被拿去比较」，不是「翻了好不好看」。**
  · 连带：撤回 LANG 之后那条 key 就没有引用了，`catalog-audit` 当场报 stale sidecar —— 记得 `extract` 一遍让它裁掉，或 `--apply` 清目录项。
- **锚文本单独 LANG，`href` 一个字节都不动** — 幽灵通知的 `(Play)` / `(View)` 链接：把整条 `<a href=…>` 收进模板会让链接跟着译文走（那 71 条含 `<a>` 的模板不登记剥标签变体就是这个道理）。写成 `"<a href='…'>[LANG(key)]</a>"`，链接留在 DM 里拼。
- **径向菜单选项的 `name` 靠「同一变量也设了 `info`」当准入证据** — `/datum/radial_menu_choice` 的 `option.name = "remove [器官名]"` / `option.info = "Remove [器官名] from the patient."` 是手术菜单的切片标签与悬停说明，纯显示（选项标识符走同一 list 里的 `OPERATION_ACTION`）。两条闸门不同，必须分开走：
  · `name`/`desc` 只收**纯字符串**、且只对 atom 开（调用点就有这道类型闸）。对所有 `.name` 放开插值模板实测新增 226 条、告警 54 → 63 —— 涌进来的是 `"\improper [区域] APC"`、`"burnt [x]"`、`"cable [x]"` 这类**运行期合成名**，它们由显示边界的加缀规则按段处理，收进目录既没用又扩大误翻面。
  · `info` 是新字段、纯显示，而且几乎都是插值模板（不收模板等于没收），所以单独一个 walker、不带 atom 闸。`name` 则在「同一个 proc 里这个变量也被赋过 `.info`」时才收模板 —— 这个结构信号把径向菜单选项和普通 atom 改名干净地分开。11 条，含 `tend bruises` 那批。
  · 顺带一条通用闸：模板去掉占位符**与 DM 文法宏**后要还剩 ≥3 个字母（`has_translatable_words`），否则 `"\improper [a] [b] [c]"` 这种纯拼装框架会当成键收进来。
- **display-return proc 的返回值模板，槽里的字面量同样要收** — 与 examine 累加器那条同源：`get_photo_description()` 返回 `"You can also see [src] on the photo[受伤 ? ", looking a bit hurt" : ""]…"`，整句进目录了，可 `{1}` 的那个分支字面量没进 —— 玩家看到「照片上你还可以看到 X, looking a bit hurt。」。
- **`x.Join()` 放进 sink 实参的裸标识符判据里，收益是负的** — 想收侦探法医报告那种「先把整篇攒进局部 list、最后一次性交给 sink」的形状，实测全仓只多抽到 1 条，而且是 `roleban_delimiter not found in href. Report this to coders.` 这种调试串。撤了。**「这个形状看着该有很多」不等于真的有** —— 先量再改。
- **「同槽兄弟」这条判据不能从 pick 词池搬到普通局部变量上** — 词池成员按构造可以互换，所以整池同性质；而一个在分支里被反复赋值的局部变量，常常一支放句子、另一支放模式键。实测放开后涌进 `imported_abilities` / `OOC` / `PRAYER` / `Warrant` / `cold` / `hot` / `low` / `high`，`nova-i18n lint` 当场 1 错误 + 8 条新碰撞。判据的适用范围要跟着**结构证据**走，不能跟着形状搬。
- **别用一个循环重写所有目录文件** — 各文件缩进不一致（`obj.json` 用制表符、`_map_names.json` 用两个空格），blanket `json.dumps(..., indent='\t')` 会把没打算动的文件整份重排：一次「改 2 条」的清理在 `_map_names.json` 上产出 10902 行 diff。而且正则写宽一点就会误伤 —— 我用 `%?PRONOUN_?[A-Za-z]*` 想清 25 条坏译文，实际改了 366 条（目录里本来就有大量合法的 `%PRONOUN_` 译文）。只改点名要改的文件、只改点名要改的 key，改完立刻看 `git diff --stat`。
- **`New()` 里做反查 = 静默返回英文，早调用哨兵是唯一抓得住的东西** — 试剂单例由 `GLOBAL_LIST_INIT(chemical_reagents_list, …)` 在 GLOB 阶段建好，那时 `ConfigLoaded` 还没读 locale。奇异试剂那条 `%MAXHEALTHRATIO%` 修法（先反查再替换）写在 `New()` 里，**看着完全正确、目录里译文也在、玩家仍是英文**：`lang_reverse_text` 在 locale 未定时按设计返回英文且不缓存。判据只有一个 —— `data/logs/ci/runtime.log` 里的 `lang_reverse_text() was called before ConfigLoaded` 哨兵（伪 locale/harvest 跑完都会留下，而单测本身**照常全绿**，「门禁失败」只体现在 runtime 计数上，极易被当成已知地图图标噪音略过）。修法是挪到 `lang_relocalize_early_string_lists()`（ConfigLoaded 之后）回调，源串取 `initial(description)`（带占位符的 canonical 形态，正是目录键）。同类：任何在 GLOB 阶段构造的单例里做的反查。
- **`is_loose_sentence` 的冒号闸门可以在 examine/扫描仪 proc 内放开占位符** — 「冒号 + 插值」在全局是日志行的典型形状（放开涌进 366 条：TGS 版本、admin 表单、PM 日志、HTML 面板），但**日志不会写在 examine 家族的 proc 里**。按 `ctx.examine` 分叉之后只进 13 条，全是医疗扫描仪报告行（`Subject contains the following reagents in their {0}stream:`、`Electrical charge: {1}`、`Time of Death: {0}`），零新增碰撞。配套把 `healthscan`/`chemscan`/`woundscan`/`diseasescan` 收进 `is_examine_proc` —— 它们整个 proc 体就是往 `render_list` 拼报告行，与 examine 累加器同性质。判据仍是**同一个 proc 内相邻两行的反差**（句号那行在目录、冒号那行不在）。
- **`response_help_*`/`response_harm_*`/`response_disarm_*` 六个字段整族没进 SINK_VARS** — 全仓 150 个值里只有 45 个**恰好**因为同名 attack_verb 入过目录，其余整齐留英文。收进来时三个词（`stamp`/`stomp`/`strike`）触发碰撞 → 按值挡（`CATALOG_VALUE_BLOCKLIST`），与 `ATTACK_VERB_IDENT_BLOCKLIST` 同一条安全线。
  · **MT 的「沿用其它命名空间既有词对」会把同形异义词照抄过来**：`brush`（树的交互动词）被填成考古刷子的「刷子」。这类的正解不是统一、也不是硬翻，而是**各自给出正确译文让歧义规则把它从反查表里摘掉**（`AMBIGUITY_ALLOWLIST` 写明理由）——物品名照旧由类型表按类型直接取键，不受影响；动词那一侧回落英文，比错译好。
- **别用一次 `write` 去「新建」master_files 里的镜像文件** — `modular_nova/master_files/` 的镜像路径多数**已经存在且已在 .dme 里**（`code/modules/reagents/chemistry/reagents.dm` 就有一份 289 行的 Nova 覆盖）。按「新建」去写会静默整份覆盖掉它，而编译照常通过（那份覆盖没了也不报错）。先 `ls` 目标目录、先 `git status` 看有没有 `D`/大面积 `M`，要加东西一律用**追加**。
- **动词池的准入证据可以是「它被喂进了 `plural_s()`」** — 投掷动词 `pick("throw","toss","hurl","chuck","fling")` 全池皆单词，卡在 pick 池那条「同池得有多词兄弟」的闸门上；模板 `You {0} {1}{2}` 早就 LANG 化也译好了，填进去的动词却一直是英文（玩家看到「你chuck廉价打火机。」）。`plural_s`/`p_s`/`p_es` 的职责是给**英文动词**补主语一致的词尾，程序查表键/act 回传值/黑板键都不会经过它 —— 于是它是「这是显示动词」的**结构性**证明，比词数启发式强，足以让全池皆单词也放行。全仓只新增 5 条、零碰撞。同一 proc 里的 `power_throw_text = " flimsily."` 与 `verb_text = "yeet"` 是单 token 的**普通赋值**（不经 pick、不经 plural_s），仍在闸门外。
- **A/B 对照实验之后必须重新 build，否则拿旧二进制量新规则** — 为了判断「某条目录项是不是我这条新规则带进来的」，我 `git stash` 了 extract.rs、重建、抽取、`git stash pop` —— 但**没有再 build**。之后所有测量都跑在旧规则的二进制上，表现为「新规则刚加的条目全被 catalog-audit 报成 stale」，看着像规则失效。判据：stale 列表里出现的正是你这次要加的那批值。
- **`LANG(key)` 单实参编译不过** — `#define LANG(key, args) (lang_format(key, args))` 是**两个**形参，少写一个是「wrong number of arguments to macro call」。无实参时写 `LANG(key, null)`（roundend.dm 里都是这个写法）。而且这条错误由 `nova-i18n extract` 的 DM 解析器先报出来，症状是抽取整个失败、不是编译失败。
